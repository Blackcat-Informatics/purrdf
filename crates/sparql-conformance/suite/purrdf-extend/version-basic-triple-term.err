# Every non-comment, non-blank line is text the failure diagnostic must contain.
# The code pins that this is the shared VERSION/admission chokepoint, not some
# other evaluation failure; the second line pins the reason (a triple term in
# term position); the spec citation pins that it is the Basic-profile gate and
# not some unrelated "triple term" mention.
native-sparql-query-eval
triple term
1.2-basic
