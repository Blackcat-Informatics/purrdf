# Competency question: "What contribution roles does GMEOW support?"
# Expected: the canonical CRediT role individuals plus legacy seeds.

PREFIX gmeow: <https://blackcatinformatics.ca/gmeow/>

SELECT ?role WHERE {
    ?role a gmeow:ContributionRole .
}
ORDER BY ?role
