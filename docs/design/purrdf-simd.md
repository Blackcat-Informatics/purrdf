<!--
SPDX-FileCopyrightText: 2026 Blackcat Informatics® Inc. <paudley@blackcatinformatics.ca>
SPDX-License-Identifier: CC-BY-4.0
-->

# PurRDF SIMD: the stable ceiling, arithmetic contracts and the hot-path audit

This document is the audit of every hot path in the PurRDF workspace against one
question: what does the compiler actually emit for it, on each target a PurRDF
build runs on? It records the rules a vectorized kernel must satisfy to ship, and
the arithmetic contracts that decide whether a float kernel may be reordered. The
evidence is always the emitted assembly, never the source.

Two things in the tree used SIMD on purpose before this audit: `memchr` on the
codec and lexer paths, and the `+simd128` wasm artifact. Everything else is what
LLVM chose to do with ordinary scalar code, and §4 measures what that is.

The measured columns of §4.1 are generated by `scripts/check-simd-asm.py
--write-doc`, and `make simd-asm` fails when the committed document and a fresh
measurement disagree. Every count in this document is therefore the count of the
tree the document ships in.

---

## 1. The stable ceiling

PurRDF builds on a nightly toolchain but uses no nightly feature, and its MSRV is a
stable release (§5). Everything below is therefore restricted to what
stable Rust offers. That is project law, not a preference.

### 1.1 Capability table

| Capability | Stable status | What PurRDF does with it |
|---|---|---|
| `#![feature(...)]` of any kind | not stable | never used; `git grep '#!\[feature'` over `crates/` and `bindings/` is empty |
| `std::simd` (portable SIMD) | not stable | not used |
| `core::arch` intrinsics | stable, per target | allowed only where the asm proves a reformulation cannot reach the same code (§2.3, rule c) |
| `#[target_feature(enable = "...")]` on a function | stable | allowed under the same rule. Such a function does not coerce to a plain `fn` pointer, so dispatch goes through ordinary wrapper functions, never through a table of `#[target_feature]` pointers |
| `is_x86_feature_detected!` / `is_aarch64_feature_detected!` | stable (std) | run-time dispatch, resolved once per scan, relation or index, never once per pair |
| wasm `simd128` (`-C target-feature=+simd128`) | stable, compile-time only | the `+simd128` wasm artifact; there is no run-time detection on wasm |
| wasm `relaxed-simd` | stable | **disqualified** for ranked and identity kernels: its results are allowed to differ between engines, so a ranking or a digest computed in one browser would not be the one computed in another. Every ranked or identity site is measured with `forbid_relaxed` |
| `f64::algebraic_add` / `algebraic_mul` / `algebraic_sub` (`float_algebraic`) | stable from Rust 1.98 | the only stable grant of reassociation and contraction for a single operation; the reassociated arithmetic of §3 is built on it, and it is why the MSRV moves (§5) |
| `-C target-cpu` / `-C target-feature` | a build flag | a consumer's choice. A library cannot make it for its consumers, so there is no workspace-wide `target-cpu` in cargo configuration; the audit measures the baseline and two raised levels per ISA instead |

### 1.2 Reproduction

The measurement is one gate. It emits assembly for seven configurations and counts
the vector work in every audited symbol:

| Configuration | Target | Codegen flag |
|---|---|---|
| `x86_64` | `x86_64-unknown-linux-gnu` | `-C target-cpu=x86-64` (SSE2) |
| `x86_64-v3` | `x86_64-unknown-linux-gnu` | `-C target-cpu=x86-64-v3` (AVX2, FMA) |
| `x86_64-v4` | `x86_64-unknown-linux-gnu` | `-C target-cpu=x86-64-v4` (AVX-512) |
| `aarch64` | `aarch64-unknown-linux-gnu` | none (NEON) |
| `aarch64-neoverse-v1` | `aarch64-unknown-linux-gnu` | `-C target-cpu=neoverse-v1` (SVE) |
| `wasm32` | `wasm32-unknown-unknown` | none (no SIMD proposal) |
| `wasm32-simd128` | `wasm32-unknown-unknown` | `-C target-feature=+simd128` |

```sh
make simd-asm                                      # measure, check the manifest and this document
python3 scripts/check-simd-asm.py --write-doc      # regenerate the measured columns of §4.1
python3 scripts/check-simd-asm.py --self-test      # every refusal and its valid neighbour, no build
python3 scripts/check-simd-asm.py --probe 'REGEX' [--config NAME] [--crate NAME] [--dump]
```

Each configuration is a release build with LTO off and one codegen unit, with
`--emit=asm` added through `CARGO_TARGET_<TRIPLE>_RUSTFLAGS`:

```sh
cargo build --release --locked --lib --target <triple> -p <audited packages> \
  --config 'profile.release.lto=false' --config 'profile.release.codegen-units=1'
```

That emits a `.s` for every crate in the graph, dependencies included, so a
dependency's own kernels (memchr's, sha2's, `regex-automata`'s) are measured in that
dependency's symbols. The asm is per crate and before LTO. LTO can only inline
further; it never removes a vector body a crate already compiled. `purrdf-wasm` is
also a `cdylib`, and linking one needs a linker for the target, which the asm does
not; it is built alone as an rlib (`cargo rustc --crate-type rlib`) against the same
dependency units.

The manifest, `scripts/simd-asm-manifest.toml`, names for each site the symbol it is
measured in on each configuration, a floor of vector operations, the mnemonics that
prove the shape, and an FMA bound. A private or inlined function is measured in the
out-of-line symbol that inlines it, and the manifest names that symbol. A missing
target standard library or tool is a failure, locally and in CI; nothing skips.

---

## 2. The three regimes and the formulation rule

### 2.1 The regimes

Every float fold in this workspace is in one of three regimes.

1. **Sequential exact.** `s += x * y` in index order. IEEE `+` is not associative,
   and LLVM is not permitted to reassociate it, so the accumulation compiles to one
   scalar add chain on every target. LLVM may still vectorize the parts that carry
   no order: the kNN dot fold was regime 1 before it moved to regime 3, and on
   `aarch64` with f32 operands its widening (`fcvtl`, `fcvtl2`) and products
   (`fmul v.2d`) were packed while the sum stayed a chain of scalar `fadd d`
   instructions. Same bits on every target; no vector accumulation anywhere. The
   PURREMB norm (`core.purremb-norm`, `knn.norm`) stays in this regime because its
   order is normative.
2. **Reassociated.** The operation grants LLVM reassociation and contraction
   (`algebraic_*`). The accumulation vectorizes and may fuse into FMA. The bits
   depend on the target, the dispatch path and the build.
3. **Fixed-lane exact.** The fold is *defined* as `LANES` independent accumulators
   followed by a fixed pairwise tree and a sequential tail. That order is the
   contract, so it is the same order on every target, it needs no grant from the
   compiler, and it vectorizes. Same bits on every target.

### 2.2 The formulation is load-bearing

Regime 3 is only regime 3 if LLVM can see the lanes. A fold written
`acc[l] += a[i * LANES + l] * b[i * LANES + l]` compiles to zero vector operations:
the index arithmetic hides the independence of the lanes. The same fold written over
`a.chunks_exact(LANES).zip(b.chunks_exact(LANES))` with an `array::from_fn`
accumulator update compiles to a packed loop. So a claim that a kernel vectorizes is
a claim about emitted asm, and every site in §4 is measured.

### 2.3 The preference order

1. **(a) Reformulate the source** so LLVM vectorizes it: `chunks_exact`,
   `array::from_fn` accumulators, bounds checks hoisted out of the loop, no dispatch
   in the inner loop. It stays portable and free of `unsafe`.
2. **(b) Offer a separate fast-variant function** under its own contract (§3).
3. **(c) Gate internally** with `core::arch` or `#[target_feature]`, only where the
   asm proves (a) cannot reach the code, and only with bit-equality under the
   function's contract.

### 2.4 What asm can and cannot prove

- `max_fma = 0` is necessary for an exact kernel and is enforced on every exact
  site. It is **not sufficient**: a fold with no FMA can still add in the wrong
  order. Summation order is proven only by the scalar reference-model tests, which
  emulate the lane, tree and tail order in plain nested loops and compare bits.
- Rust never sets LLVM's `contract` fast-math flag on a plain `*` or `+`. So
  `#[target_feature(enable = "fma")]` on an exact kernel cannot fuse it; FMA appears
  only where an `algebraic_*` operation or an explicit `mul_add` asks for it.
- A count is the whole symbol a site is measured in. When a site is inlined into a
  larger function, that function's other vector work (hashbrown's SSE2 or NEON
  group probes, straight-line struct moves, a UTF-8 width computation) lands in the
  same count. The *proof mnemonics* are what tie a count to the audited loop, which
  is why every `covered` row requires them and no `leave` or `rewrite` row claims
  vector work from a bare count.

---

## 3. Contract law

### 3.1 An arithmetic contract is a type

`purrdf_core::distance::Arithmetic` is a sealed trait with two implementations:

- **`Exact`**, the regime-3 law: sixteen binary64 accumulators over the whole
  sixteen-element chunks (`as_chunks::<16>`, the array form of `chunks_exact(16)`,
  rebuilt each step with `array::from_fn`), the pairwise tree `(l, l+8)`, `(l, l+4)`,
  `(l, l+2)`, `(0, 1)`, then the remainder folded sequentially in ascending index. Its identifier is
  `binary64-lane16-tree-v1`, a plain identifier and not an IRI.
- **`Reassociated`**, regime 2, identifier `binary64-reassociated-v1`.

Every consumer (the kernels, the exact kNN relation and HNSW) is generic over
`A: Arithmetic` with `Exact` as the default. Monomorphization gives two separate
functions per site, so there is no mode. Mixing arithmetics is a type error, and the
evidence strings, identifiers and guard tables are derived from `A`'s constants
rather than kept by hand. A further arithmetic (binned, quantized) is one more
`impl`.

### 3.2 Two functions, two contracts

The named entry points stay: `Kernel::distance` is `Exact`, and
`Kernel::distance_reassociated` is `Reassociated`. Each takes the resolved handle of its
own arithmetic (`Resolved<Exact>`, `Resolved<Reassociated>`), so passing one law's handle
to the other's entry point is a type error, and neither runs without the environment check
(§3.7). The exact kernel keeps its name and its promise. There is never a mode argument, an environment variable or a
switch that turns one into the other.

### 3.3 The fast contract names its divergence

A reassociated result carries evidence text that says what it gives up: sums are
reassociated and may be contracted to FMA along a named dispatch path, results may
differ in the last bits from the exact arithmetic and between dispatch paths or
builds, the sign of a zero result is unspecified, and near-ties may order
differently. The text is pinned by a test.

### 3.4 No substitution, in either direction

An exact call never runs reassociated code and a reassociated call never silently
falls back to exact code. Dispatch happens only inside one contract: `Exact` may
choose between a portable, an AVX2 and an AVX-512F compilation of the *same* generic
body (identical bits by construction and by test), and `Reassociated` may choose
between its own SSE2, AVX2+FMA and AVX-512 paths. The exact AVX-512F compilation is
kept on a measurement, not an argument: under generic tuning its sixteen lanes are two
`zmm` registers rather than AVX2's four `ymm`, so a sixteen-element block of the
squared-Euclidean fold is two `vsubpd`, two `vmulpd` and two `vaddpd` where AVX2 issues
four of each -- the same shape, with no FMA -- and every out-of-line kernel is shorter
(the `distance.exact.*` rows of §4.1 carry the counts). Both arithmetics select their
`avx512f` path by one rule: the processor reports AVX-512F, AVX2 and FMA. Every target has a reassociated path, so a
reassociated call is never refused for its target: x86_64 dispatches among those
three, aarch64 runs NEON and wasm its `simd128` or scalar build, and every other
target runs the `portable` path, the same body compiled once for the target's
baseline features. Dispatch is resolved once per scan, batch or index, never once per
pair.

### 3.5 Where the choice is recorded

The choice of arithmetic is recorded wherever it outlives the call:

- the HNSW image header carries an arithmetic field (formerly a reserved `u32` that
  had to be 0), holding the code of the path resolved at build time (`1` for `Exact`
  on every path; for `Reassociated` `2` sse2, `3` avx2+fma, `4` avx512f, `5` neon,
  `6` wasm-simd128, `7` wasm-scalar and `8` portable); decode refuses a code that is
  not one of the index type's own. Decode, search and rebuild verification resolve the
  recorded path itself (`Arithmetic::resolve_recorded`), not the widest this process
  runs, so an image recorded on a narrower path the processor also runs -- SSE2 or
  AVX2+FMA on an AVX-512F processor -- runs on that path; only a path this process
  cannot run (another target's compilation, or a processor without a feature it
  needs) is a named refusal;
- the path does not pin the code that ran. Every path is also compiled under the
  consumer build's own `-C target-cpu`/`-C target-feature`, and the `distance.reassociated.dot`
  row in §4.1 measures the consequence: the `avx512f` path is `zmm` FMA under generic
  tuning and `ymm` in an x86-64-v4 build, the `sse2` path is itself FMA code in an
  x86-64-v3/v4 build (and in any build with `target-cpu=native` on an FMA processor),
  and the `neon` path is SVE `fmla` + `faddv` in a neoverse-v1 build. Two builds recording
  one path can compute different bits. So a reassociated image header also records,
  right after its code, the build's `BuildShape` (`Arithmetic::build_shape`): a versioned
  `u64` naming the target architecture and the `cfg(target_feature)` set that decides
  the reassociated body's vector and FMA code (on `x86_64` `sse3` through `avx512vl`
  and `fma`; on `aarch64` `neon`, `sve`, `sve2` and their neighbours; on wasm
  `simd128` and `relaxed-simd`; the layout table is in `distance/shape.rs`). Decode,
  search and rebuild verification refuse another build's shape with the named
  `HnswError::ArithmeticBuildMismatch`, whose message lists both feature sets. `Exact`
  has no shape and its image carries none: its bytes are the same in every build.
  The feature bits are not the whole build: `rustc` exposes no `cfg` for
  `-C target-cpu`, CPU tuning (x86-64-v4's 256-bit preference, a Neoverse
  scheduling model) is not a target feature, and neither the compiler release nor the
  optimisation level is a `cfg`. So the shape also carries a `BuildIdentity`, recorded
  as a second `u64` after the bits: the FNV-1a digest of one line of text that
  `purrdf-core`'s build script writes from what `cargo` hands it -- `rustc -vV`'s
  release, commit hash and LLVM version (run on `RUSTC`, never through a wrapper), the
  target CPU (`-C target-cpu` as named, `native` resolved to the host CPU `rustc`
  reports, else the target's default from `rustc --print target-cpus`), every
  `-C target-feature` as `rustc` applies them (tuning features such as
  `prefer-256-bit` included), any `-C llvm-args`, `codegen-units`, `lto` or
  `overflow-checks` in the rustflags, and the optimisation level -- followed by the
  crate's own `cfg(debug_assertions)`. A build of another identity is the same named
  `ArithmeticBuildMismatch`, whose message shows both identities and says which half of
  the shape differs. Under an equal path and shape the rebuild compiles the body to the
  code that computed the image, so a rebuild that differs is `false`, the same tamper
  evidence as an exact image's. What no build script sees is named on `BuildIdentity`:
  a `[profile]` table's own `lto`, `codegen-units` and `overflow-checks`, flags a
  `cargo rustc --` invocation appends, and a compiler whose `rustc -vV` does not tell it
  apart from another; builds that differ only there share an identity;
- the HNSW profile declaration folds the arithmetic identifier, so the profile
  digest binds the law, and a reassociated declaration ends with a
  `build-shape=<bits>` line and a `build-identity=<digest>` line, so its digest binds
  the build too;
- HNSW is `HnswIndex<A: Arithmetic = Exact>`, with `HnswSpace<A>` and
  `HnswRelation<A>` over it: `build` and `decode` stay exact, and
  `build_reassociated` / `decode_reassociated` are the reassociated index. A
  reassociated index publishes its own implementation, `hnsw-reassociated-v2`, whose
  evidence revision is `LOSS_EVIDENCE` followed by the reassociated evidence for the
  path the build ran and the sentence that its canonical image is reproducible only by
  a build of the shape that made it, running the same dispatch path, since the image
  records that build's target, features and identity and a build of another shape
  refuses it (`profile::loss_evidence_reassociated`). The
  guard's legal (identifier, revision, image codes) rows are derived from `Exact` and
  `Reassociated`, and a cross pairing is a profile failure. `HnswSpace::evidence()`
  returns that text, `HnswRelation::fidelity` carries it into `Completeness::Lossy`
  and composes the order axis with `OrderFidelity::Perturbed` carrying the
  arithmetic's own evidence, and the ranked declaration names the law;
- the kNN relation is `EmbeddingKnnRelation<A: Arithmetic = Exact>`: `new` builds the
  exact relation and `new_reassociated` the reassociated one, resolving its dispatch
  path once at construction (a flushing float environment is refused there, and on
  every search, with the named `EvalError::FloatEnvironment`), and the scan runs
  `A`'s batch kernel on that path. Its ranked declaration names the law in
  `RankedDeclaration::arithmetic` as `RankArithmetic::FloatDistance`, which
  `canonical_description` folds, so an exact
  and a reassociated producer over one space have different registry content
  fingerprints and so different plan ids. A `RankArithmetic::FloatFree` declaration
  (the lexical producer, which ranks fixed-point integers) describes itself in the
  bytes it always did. A reassociated relation also composes the host's order
  fidelity with `OrderFidelity::Perturbed`, carrying the evidence text for its
  resolved path verbatim, through `composed_order_fidelity`, the one composition
  the HNSW relation also uses. The completeness axis stays the host's, because a
  reassociated scan still scores every row;
- the fusion trailer (`FusionTrailer::fidelities`, `RankFidelity`) carries that
  fidelity into any fused ranking, and an answer with a reassociated stratum reports
  it as having no finite score bound.

Two records deliberately do **not** change. `DistanceMetric` names the metric's
semantics, which are unchanged; PURREMB §7.4 permits a kernel to optimize the
evaluation as long as it preserves the metric and row-number tie-breaking, so the
family-contract digest is the same for both arithmetics and a test asserts it.
`IndexLossContract` stays `transforms_vectors: false`: an arithmetic does not
transform the stored vectors, and `purrdf-core` requires `loss_encoding: None` in
that case, so the choice lives in the image field and the profile instead.

### 3.6 Identity paths are exact

Content identity, canonical serialization, GTS and RDFC-1.0 are exact by contract.
A kernel on those paths is regime 3 or bit-exact integer code, never reassociated.
This is enforced statically: `algebraic_add` and its siblings, and the
`Reassociated` arithmetic, may appear only under `crates/rdf-core/src/distance/`,
`crates/sparql-eval/src/knn/`, `crates/hnsw/src/` and those crates' tests and
benches. A hit anywhere else fails `make simd-asm`.

The PURREMB norm is the one float fold on an identity path. PURREMB §13.2 makes its
written order normative (no FMA, sequential), so `norm_fold` is never reordered and
has no reassociated sibling.

### 3.7 The float-environment precondition

Both arithmetics assume IEEE round-to-nearest, ties-to-even, with subnormals
preserved. A thread that has set flush-to-zero or denormals-are-zero, or another
rounding direction (MXCSR on x86_64, FPCR on aarch64, `frm` on RISC-V, FPSCR on
32-bit Arm and POWER), would compute different bits from the same code. So would a
unit that rounds twice: where `f64` arithmetic runs on the x87 (`i586`, or `i686`
built without SSE2), each result is rounded to the register's 64-bit significand and
again when it is stored, and a value kept in a register has a 15-bit exponent, so it
neither overflows nor becomes subnormal where binary64 would.

The x87 is made correct, not refused -- and not only here. Every binary floating-point
operation in the workspace whose bits reach a result runs through one layer,
`purrdf_xsd::ieee`, the lowest crate every consumer depends on: both distance
arithmetics, the PURREMB norm and the probe (as `Binary64` methods inside a `Precision`
scope, `distance::binary64`, a thin wrapper over `ieee::Binary64Scope`), and beyond the
distance module the `xsd:double` and `xsd:float` operators (`numeric_add`, `_sub`, `_mul`,
`_div`, so SPARQL `+ − × ÷`, `SUM`, `AVG`, `VARIANCE`), `fn:round`, the decimal and
big-integer conversions' exact paths, `STDDEV`'s square root, the join planner's cost
model (whose estimate orders an unordered query's rows and decides a governed refusal),
the normalized-projection reads, and the CSVW percentage scaling. `+`, `−`, `×`, `÷` and
`√` at both widths are correctly rounded; the transcendentals are not required to be by
IEEE-754, and no result is computed with one. On every other target each operation is
the language's operator, `#[inline(always)]`, and each scope is empty (the reassociated
body keeps its `algebraic_*` operations there; they are defined in `distance::binary64`,
not in the exact-only `ieee` layer, and on the x87 they are its correctly rounded `+`,
`−`, `×` in written order). On the x87 a `Binary64Scope` saves the
control word and sets its precision-control field to 53 bits (a `Binary32Scope` to 24),
restoring the caller's word on exit, unwinding included; each operation is one `asm!`
block that loads its operands, performs one instruction and stores a result of the
operation's width, so no value outlives its operation at the register's precision or
range. That makes `+`, `−` and `√` correctly rounded: a normal result already has the
format's width, an overflow is stored as the infinity IEEE gives, and a sum or
difference in the subnormal range is exact. It does not make `×` and `÷` correct in the
subnormal range, where the result is rounded again by the store (Monniaux, *The pitfalls
of verifying floating-point computations*, 2008). The technique `HotSpot` used for Java's
`strictfp` closes that: one operand is scaled by the distance between the x87's smallest
normal exponent (−16382) and the format's -- 2⁻¹⁵³⁶⁰ for binary64 (−1022), 2⁻¹⁶²⁵⁶ for
binary32 (−126) -- so the x87 denormalizes the result onto the format's subnormal grid
(scaled) and rounds once; scaling back and storing are exact. On the x87 the reassociated
body runs these same operations in written order: the licence permits reassociation and
requires none, and every value the fold holds is then binary64, as its evidence claims.
An integer software reference that rounds without any floating-point unit
(`ieee::reference`) is the oracle: the operations at both widths, the probe constants,
the exact kernels, the norm, and the SPARQL operators and aggregates on double-rounding
witnesses equal it bit for bit on every target, and the CI runs the `purrdf-xsd` unit
tests, the SPARQL arithmetic tests and the distance, kNN and HNSW suites on `i586`. A
thread that loaded a directed rounding control into its x87 control word is refused by
name at every distance entry point, on the thread that computes, rayon workers included.

The law is behavioural, and it holds on every target. Resolving an arithmetic runs a
probe of thirteen binary64 operations whose correctly rounded results under the
assumed environment are known constants, computed through the same `Binary64`
operations and inside the same `Precision` scope as every kernel, with every operand
passed through `core::hint::black_box` so none is folded at compile time:

| Operation (`ulp` = 2⁻⁵²) | IEEE-754 result | A mismatch shows |
|---|---|---|
| `f64::MIN_POSITIVE × 0.5` | subnormal 2⁻¹⁰²³ | subnormal results flushed |
| `2⁻¹⁰⁷⁴ × 2¹⁰²³` | normal 2⁻⁵¹ | subnormal operands read as zero |
| `1 + 0.75 ulp` | `1 + ulp` | rounding toward zero or down |
| `1 + 0.25 ulp` | `1` | rounding up |
| `−1 − 0.75 ulp` | `−(1 + ulp)` | rounding toward zero or up |
| `−1 − 0.25 ulp` | `−1` | rounding down |
| `1 + 0.5 ulp` | `1` | a tie rounded away from zero, or up |
| `(1 + ulp) + 0.5 ulp` | `1 + 2 ulp` | a tie rounded toward zero, or down |
| `1 + (2⁻⁵³ + 2⁻⁷⁸)` | `1 + ulp` | a sum rounded twice |
| `(1 + ulp) + (2⁻⁵³ − 2⁻⁷⁸)` | `1 + ulp` | a sum rounded twice |
| `(1 − 0.5 ulp) × (1 + 2 ulp)` | `1 + ulp` | a product rounded twice |
| `2⁻⁵¹²(1 + 4 ulp) × 2⁻⁵¹¹(1 − ulp)` | subnormal `2⁻¹⁰²³ + 2⁻¹⁰⁷⁴` | a subnormal product rounded twice |
| `2⁻⁵¹² ÷ 2⁵¹¹(1 − ulp)` | subnormal `2⁻¹⁰²³ + 2⁻¹⁰⁷⁴` | a subnormal quotient rounded twice |

The last five rows are double-rounding witnesses: each exact result lies within a
64-bit rounding of a binary64 midpoint without being one, so rounding it first to the
x87's 64 bits lands on the midpoint and the tie then goes to the wrong neighbour.
Precision control at 53 bits closes the three normal-range rows; only the scaling
closes the two subnormal ones. Every unit that rounds once passes all five, and the
software reference shows that none of the first eight rows can tell double rounding
apart. The first row whose bits differ is refused as `FloatEnvironmentError::FlushToZero`,
`RoundingMode` or `DoubleRounding`, with `FloatEnvironmentEvidence::Probe` naming the
operation and both bit patterns; a thread where the precision guard did not take hold
is refused as `DoubleRounding` rather than handed doubly rounded distances. The probe is binary64 only because binary64 is the only precision the
arithmetics execute: `f32` operands are widened per component before any operation.

Where this build reads the control register — MXCSR on x86_64, FPCR on aarch64, and the
x87 control word where the x87 is the binary64 unit — it reads it first, so a refusal
names the register and its value (`FloatEnvironmentEvidence::Register`), and then runs
the probe as well, which proves the environment rather than the bits the build knows
the meaning of. The x87 control word is refused for a directed rounding-control field
only: its precision-control field is the guard's to set, so a caller that left it at 24
or 64 bits is served correctly rounded binary64, not refused. WebAssembly's
numeric instructions are specified to round to nearest-even and preserve subnormals;
the probe runs there too, since it is thirteen operations and an engine departing from
the specification is then refused by name rather than trusted. No target is refused
for being unread: an environment is refused only when it has been observed to depart.

The check cannot be bypassed by choosing a different entry point, because every public
entry point that computes a distance takes the resolved handle the check produces
(`Resolved<A>`): the batch and pair kernels on `Resolved` in `purrdf-core`,
`Kernel::distance`, `Kernel::distance_bounded` (a `Resolved<Exact>`) and their
`_reassociated` siblings (a `Resolved<Reassociated>`) in `purrdf-sparql-eval`, and
`VectorMatrix::distance`, `distance_from_query` and `distance_bounded` (a `Resolved<A>`)
in `purrdf-hnsw`. There is no handle-free distance. A flushing thread is refused by name
when it asks for the handle, so it never obtains one and no distance, per-pair or batch,
is computed on it; `crates/hnsw/tests/float_environment.rs` executes that refusal and, as
its valid neighbour, every pair entry point in the default environment agreeing bit for
bit with the batch kernel on a pair whose distance is subnormal, so a flushing thread
would have returned zero for it.

Nor can it be bypassed by choosing a different thread. The environment is per-thread
control state: MXCSR and FPCR are saved and restored with the thread, so a handle
resolved on a clean thread proves nothing about another. `Resolved<A>` is therefore
neither `Send` nor `Sync` (a `PhantomData<*const ()>` marker beside the path), and the
compiler refuses to move one, or share a reference to one, across threads; two
`compile_fail` doctests on the type pin both refusals, beside a passing one that sends
the thread-free selection instead. What outlives a thread is `Selected<A>`, the
arithmetic and its dispatch path with no environment proof, which is `Send`, `Sync` and
computes nothing: `Selected::resolve` repeats the environment check on the calling
thread (the path was selected once, from the process's processor features) before it
hands out a handle. Every long-lived structure stores that: `HnswIndex` its recorded
path (`HnswIndex::arithmetic`), and the kNN relation and its cursor the path a
reassociated relation selected at construction (`EmbeddingKnnRelation::selected`).
Every compute entry resolves on the thread that computes: HNSW search, `search_batch`,
`row_distance`, `verify_rebuild` and the kNN scan and membership lookup once per call,
and `search_batch` and the build's parallel proposals once inside each rayon worker,
per worker's share rather than per query or pair.
`crates/hnsw/tests/float_environment.rs` builds every index and relation on the test
thread and executes the refusal from a worker thread that set flush-to-zero on itself
(`search_batch` and `row_distance` on both indexes, the scan and the membership lookup
on both kNN relations), with the same calls from a clean worker answering the test
thread's bits as the valid neighbour; `crates/hnsw/tests/float_environment_rayon.rs`
makes rayon's global pool flush-to-zero and shows a batch called from a clean thread
refused by its workers, while the same batch on a clean pool answers the single-thread
bits.

The L2 norm a cosine kernel divides by is arithmetic too, and follows the same law. Its
only public form is `Resolved::<Exact>::norm` in `purrdf-core` (re-exported through
`knn`), a method of the exact handle alone: PURREMB §13.2 gives the fold one written
order, so it has no reassociated form, and a `Reassociated` consumer obtains the exact
handle with `Resolved::exact`, which carries the environment its own handle proved. The
other public norm entry points take that handle as well: `VectorMatrix::norm_of_row` in
`purrdf-hnsw`, and PURREMB's deterministic-L2 projection rows,
`EffectiveMatrixView::f32_row` and `f64_row` in `purrdf-core` (and
`guard::read_effective_matrix`, which reads through them). The artifact writer and
`verify_embedding` resolve the exact handle once where a normalized projection is present
and refuse a flushing thread with `EmbeddingError::FloatEnvironment`; a raw projection
folds stored bytes and no arithmetic, so it is written and verified on any thread. The
same suite executes the norm refusal on a row whose norm is subnormal (`[3e-310,
4e-310]`, which a flushing thread folds to `+0`) and, as the valid neighbour, every norm
entry point returning the reference fold's nonzero bits once the register is restored;
`crates/rdf-core/tests/purremb_float_environment.rs` does the same for the writer, the
verifier and the normalized row, with a raw artifact written and verified under the
flushed register as the neighbour.

The probe runs on every resolve, which is once per scan, search, lookup, rebuild, pair
call site or rayon worker's share of a batch, never once per pair: the handle is `Copy`,
and a loop on one thread passes the one it holds.
A resolve is a handful of operations and no allocation. The `cross-arch` CI job checks the kernels on
i686, armv7, riscv64, powerpc64le, s390x and loongarch64 and executes the distance,
kNN and HNSW suites on i686, and `cross-arch-riscv64` executes them on riscv64 under
emulation; on both no register is read and the probe alone decides.

---

## 4. Inventory

### 4.1 Site table

One row per site. `crate` is the workspace member the row belongs to; `fn` and
`path` locate the source; `covers` names the minimum-coverage roster entries the row
answers for; `class` is **I** (integer or byte code), **FI** (float whose bits are
identity or normative) or **FR** (float used for ranking). The verdicts:

- **covered**: the site already compiles to vector work, or deliberately to a
  SWAR or popcount path; its manifest entry carries floors and proof mnemonics;
- **rewrite**: the site was reformulated (§2.3) so LLVM vectorizes it;
- **regime-3**: a float fold on the fixed-lane exact law, with dispatch inside
  `Exact`;
- **fast-variant**: a site with a separate reassociated entry point;
- **leave**: nothing vector-shaped is left to take, for the stated reason.

Each configuration cell is generated: per measured symbol, `v` vector operations
(the minimum over the symbol's copies), `f` fused multiply-adds and `r` `relaxed_`
wasm instructions (the maxima), and `req✓` where the required mnemonics are present.
A `leave` entry has a floor of 0 and requires no mnemonic, so its cells are a record
of what the site compiles to, subject to the whole-symbol caveat of §2.4. A
`rewrite` entry carries the floors and proof mnemonics of its reformulated shape; a
rewritten site with nothing vector-shaped left in it (a cursor, a table load) keeps a
floor of 0 and says so. A crate-level row names no function and has no cells.

<!-- simd-asm:sites:begin -->
| id | crate | fn | path | benches | covers | class | blocker | verdict | reason | simd128 | x86_64 | x86_64-v3 | x86_64-v4 | aarch64 | aarch64-neoverse-v1 | wasm32 | wasm32-simd128 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `memchr.sparql-lex-string` | `purrdf-sparql-algebra` | `Lexer::lex_string` (`memchr2` clean-run skip) | `crates/sparql-algebra/src/lexer.rs:497` | `tokenize`, `tokenize_values`, `update_reparse` | sparql-tokenizer | I | — | covered | memchr's own kernels: dispatched at run time to `find_sse2` / `find_avx2` on x86_64, inlined into `Lexer::lex_one` on aarch64 and wasm32 | +simd128: memchr's `i8x16.eq` + `i8x16.bitmask` kernel inlined into `lex_one`; baseline: memchr's SWAR fallback, inlined into `run` | sse2 v30·f0·r0·req✓<br>avx2 v36·f0·r0·req✓ | avx2 v38·f0·r0·req✓ | avx2 (avx512 masks) v16·f0·r0·req✓ | neon v132·f0·r0·req✓ | neon v132·f0·r0·req✓ | swar v0·f0·r0 | simd128 v186·f0·r0·req✓ |
| `sparql.lexer-trivia` | `purrdf-sparql-algebra` | `Lexer::skip_trivia`, `terminals::find_first_trivia` | `crates/sparql-algebra/src/lexer.rs:292` | `tokenize`, `tokenize_values`, `update_reparse` | sparql-tokenizer | I | — | rewrite | a whole `WS` run is skipped by one `terminals::find_first_trivia` call (`iri.scan-trivia`), the comment tail by one `memchr`, so no per-byte loop is left. The scanner is one out-of-line copy in `purrdf_iri` and its vector work is measured there; the call-site cells are `Lexer::run`, whose count includes the inlined comment-tail `memchr`. The old byte loop is kept under test as the oracle, compared at every char boundary of fixed-seed random text | the scanner: +simd128 packed `i8x16` compares, baseline scalar lanes; the call site: +simd128 the comment-tail `memchr` kernel (`i8x16.eq`, `i8x16.bitmask`), baseline scalar | scan sse2 v10·f0·r0·req✓<br>call site (in run) v0·f0·r0 | scan avx2 v22·f0·r0·req✓<br>call site (in run) v13·f0·r0 | scan avx512 masks v18·f0·r0·req✓<br>call site (in run) v8·f0·r0 | scan neon v8·f0·r0·req✓<br>call site (in run) v18·f0·r0 | scan neon v8·f0·r0·req✓<br>call site (in run) v18·f0·r0 | scan scalar v0·f0·r0<br>call site (in run) v0·f0·r0 | scan simd128 v8·f0·r0·req✓<br>call site (in run) v18·f0·r0 |
| `sparql.lexer-iri` | `purrdf-sparql-algebra` | `Lexer::lex_lt_or_iri` | `crates/sparql-algebra/src/lexer.rs:429` | `tokenize`, `tokenize_values`, `update_reparse` | sparql-tokenizer | I | — | rewrite | one pass: `terminals::find_first_iri_body_special` (`iri.scan-iri-body`) stops at the first byte the production forbids raw, and that byte decides the token: `>` borrows the body verbatim, `\` hands over to the `UCHAR` decoder, anything else (or no stop) makes `<` an operator. The scanner is one out-of-line copy in `purrdf_iri`, measured there; the call-site cells are `Lexer::run` on x86_64 and wasm32 and `Lexer::lex_one` on aarch64 and wasm32-simd128, which also hold the string-literal `memchr` kernels. The three-pass scan is kept under test as the oracle, compared at every `<` of fixed-seed random text | the scanner: +simd128 packed `i8x16` compares, baseline scalar lanes; the call-site symbols also hold memchr's kernels (`i8x16` under +simd128, SWAR on baseline) | scan sse2 v17·f0·r0·req✓<br>call site (in run) v0·f0·r0 | scan avx2 v16·f0·r0·req✓<br>call site (in run) v13·f0·r0 | scan avx512 masks v8·f0·r0·req✓<br>call site (in run) v8·f0·r0 | scan neon v14·f0·r0·req✓<br>call site (in lex_one) v132·f0·r0 | scan sve v11·f0·r0·req✓<br>call site (in lex_one) v132·f0·r0 | scan scalar v0·f0·r0<br>call site (in run) v0·f0·r0 | scan simd128 v13·f0·r0·req✓<br>call site (in lex_one) v186·f0·r0 |
| `sparql.lexer-cursor` | `purrdf-sparql-algebra` | `Lexer::cur`, `Lexer::peek` | `crates/sparql-algebra/src/lexer.rs:221` | `tokenize`, `tokenize_values`, `update_reparse` | sparql-tokenizer | I | — | rewrite | an ASCII fast path: `cur` reads the byte and decodes only a non-ASCII lead; `peek` answers from the byte at `pos + ahead` when the window up to it is ASCII (`<[u8]>::is_ascii`) and decodes otherwise. A lookahead is one or two bytes, so there is nothing vector-shaped to take and the entry records the scalar result; `peek` is out of line, `cur` is measured in `Lexer::run`. The decoding cursor is kept under test as the oracle at every char boundary and every lookahead 0-3 | scalar on both builds (the `peek` window test is std's `is_ascii`) | peek v4·f0·r0<br>cur (in run) v0·f0·r0 | peek v4·f0·r0<br>cur (in run) v13·f0·r0 | peek v3·f0·r0<br>cur (in run) v8·f0·r0 | peek v5·f0·r0<br>cur (in run) v18·f0·r0 | peek v5·f0·r0<br>cur (in run) v18·f0·r0 | peek v0·f0·r0<br>cur (in run) v0·f0·r0 | peek v0·f0·r0<br>cur (in run) v18·f0·r0 |
| `memchr.rdf-line-split` | `purrdf-rdf` | `split_line_chunks`, `count_line_terminators`, `PhysicalLines` | `crates/rdf/src/native_codecs/text_parse.rs:409` | `native_codecs`, `stream_parse_alloc`, `gts_ingest` | rdf-line-split | I | — | covered | the line-oriented codecs split on memchr's `\n` / `\r` kernels: dispatched on x86_64, inlined into `parse_lines_parallel` and `PhysicalLines::next` on aarch64 and wasm32 | +simd128: `i8x16.eq` + `i8x16.bitmask` in both symbols; baseline: SWAR | sse2 v30·f0·r0·req✓<br>avx2 v36·f0·r0·req✓ | avx2 v38·f0·r0·req✓ | avx2 (avx512 masks) v16·f0·r0·req✓ | neon v44·f0·r0·req✓<br>neon (lines) v22·f0·r0·req✓ | neon v44·f0·r0·req✓<br>neon (lines) v22·f0·r0·req✓ | swar v0·f0·r0<br>swar (lines) v0·f0·r0 | simd128 v67·f0·r0·req✓<br>simd128 (lines) v30·f0·r0·req✓ |
| `rdf.escape-scan` | `purrdf-rdf` | `ser_model::escape_iri` (via `iri_escape::escape`), `escape_literal`, `find_first_literal_escape` | `crates/rdf/src/native_codecs/ser_model.rs:335` | `native_codecs` | rdf-escape | I | — | rewrite | one chunked scan for the next byte that can begin an escape, then a bulk copy of the run before it; a candidate that needs no escape stays in the run, so a value with nothing to escape (non-ASCII included) is borrowed. `escape_iri` is `purrdf_core::iri_escape::escape`, the one IRIREF egress (scan: `iri_escape::find_first_candidate`, the ASCII escaped bytes plus the `0xC2` lead of the C1 block); `escape_literal` keeps its own law (C1 escaped, no `\b`/`\f`, for XML transport) over a local class (`"`, `\`, C0, DEL, `0xC2`). The kernel is `purrdf_iri::terminals::ByteClass`, the formulation of `iri.scan-json-string` exported for a class a writer owns: sixteen-byte `as_chunks` lanes, each a `0x00`/`0xFF` byte from the class runs written as compares, a branch-free OR-fold as the clean-chunk test, the hit chunk's lanes read as a little-endian `u128` whose trailing-zero count over 8 is the offset. The frozen per-char oracles are compared over dense fixed-seed values at every offset 0-3 and lengths 0-70 plus longer, and borrowing is asserted exactly when nothing changed; the serializer goldens are unchanged | the scans: +simd128 packed `i8x16` compares, baseline scalar lanes; the drivers: scalar on both builds | iri scan sse2 v23·f0·r0·req✓<br>iri (in iri_escape::escape) v0·f0·r0<br>literal scan sse2 v12·f0·r0·req✓<br>literal v0·f0·r0 | iri scan avx2 v22·f0·r0·req✓<br>iri (in iri_escape::escape) v0·f0·r0<br>literal scan avx2 v11·f0·r0·req✓<br>literal v0·f0·r0 | iri scan avx512 masks v10·f0·r0·req✓<br>iri (in iri_escape::escape) v0·f0·r0<br>literal scan avx512 masks v5·f0·r0·req✓<br>literal v0·f0·r0 | iri scan neon v20·f0·r0·req✓<br>iri (in iri_escape::escape) v0·f0·r0<br>literal scan neon v10·f0·r0·req✓<br>literal v0·f0·r0 | iri scan sve v13·f0·r0·req✓<br>iri (in iri_escape::escape) v0·f0·r0<br>literal scan neon v10·f0·r0·req✓<br>literal v0·f0·r0 | iri scan scalar v0·f0·r0<br>iri (in iri_escape::escape) v0·f0·r0<br>literal scan scalar v0·f0·r0<br>literal v0·f0·r0 | iri scan simd128 v19·f0·r0·req✓<br>iri (in iri_escape::escape) v0·f0·r0<br>literal scan simd128 v9·f0·r0·req✓<br>literal v0·f0·r0 |
| `rdf.sort-canonical` | `purrdf-rdf` | `SerGraph::sort_canonical` | `crates/rdf/src/native_codecs/ser_model.rs:135` | `native_codecs` | — | I | a comparison sort over rendered statements | leave | the cost is re-rendering and allocation for the sort keys, not a data-parallel loop | scalar on both builds | v0·f0·r0 | v0·f0·r0 | v0·f0·r0 | v0·f0·r0 | v0·f0·r0 | v0·f0·r0 | v0·f0·r0 |
| `rdf.viz-layout` | `purrdf-rdf` | `viz::layout::layout_scene` | `crates/rdf/src/viz/layout.rs:250` | `viz_layout` | — | I | layered layout over `BTreeMap<String, i64>` position maps with crossing-reduction sweeps | leave | pointer-chasing string-keyed maps; no float arithmetic and no contiguous loop to widen | +simd128 packs some `i32x4` lane arithmetic in the symbol, as the x86 and aarch64 builds do; the position maps are `BTreeMap` walks on every build | v16·f0·r0 | v37·f0·r0 | v37·f0·r0 | v22·f0·r0 | v24·f0·r0 | v0·f0·r0 | v12·f0·r0 |
| `rdf.projections` | `purrdf-rdf` | `projections::carrier::project_archive` | `crates/rdf/src/projections/carrier.rs:420` | `projections` | — | I | carrier assembly over `BTreeMap` models and the `csv` crate's record writer | leave | allocation and map-building bound; field quoting is the `csv` crate's | scalar on both builds | v61·f0·r0 | v20·f0·r0 | v20·f0·r0 | v0·f0·r0 | v1·f0·r0 | v0·f0·r0 | v0·f0·r0 |
| `rdf.csvw-percent-encode` | `purrdf-rdf` | `csvw::rdf::percent_encode_reserved` (`find_first_reserved_escape`), `percent_encode` (`find_first_unreserved_escape`), both inlined into `expand_url` | `crates/rdf/src/projections/csvw/rdf.rs:463` | `projections` | — | I | — | rewrite | measured before the rewrite as not covered: the reserved expansion (`{+name}`) found its first byte to encode with `position` over `reserved_byte`, a byte-at-a-time loop, and wrote every later byte one at a time, and the simple expansion (`{name}`) was a per-byte loop throughout. Each now finds every plain run with a `ByteClass` over the bytes it encodes, the complement of its admitted set (`reserved_byte`: RFC 3986 unreserved and reserved characters and `%`, so nine runs; `unreserved_byte`, seven runs), each compiled out of line as one kernel; the run is copied whole and the stop byte written as `%XX`. Both classes hold every non-ASCII byte, so every run is ASCII and starts and ends on a `char` boundary. The per-byte encoders are the oracles over every ASCII scalar and non-ASCII scalars of each UTF-8 width, after every plain prefix of 0 to 40 bytes; both tables are checked against their predicates on all 256 bytes. `csvw_url_templates/read_2000_rows` in `projections` runs both expansions on every row | the scans: +simd128 packed `i8x16` compares, baseline scalar lanes; the driver: scalar on both builds | reserved sse2 v22·f0·r0·req✓<br>simple sse2 v25·f0·r0·req✓<br>call site v0·f0·r0 | reserved avx2 v21·f0·r0·req✓<br>simple avx2 v24·f0·r0·req✓<br>call site v0·f0·r0 | reserved avx512 masks v9·f0·r0·req✓<br>simple avx512 masks v10·f0·r0·req✓<br>call site v0·f0·r0 | reserved neon v18·f0·r0·req✓<br>simple neon v19·f0·r0·req✓<br>call site v0·f0·r0 | reserved sve v12·f0·r0·req✓<br>simple sve v15·f0·r0·req✓<br>call site v2·f0·r0 | reserved scalar v0·f0·r0<br>simple scalar v0·f0·r0<br>call site v0·f0·r0 | reserved simd128 v17·f0·r0·req✓<br>simple simd128 v18·f0·r0·req✓<br>call site v0·f0·r0 |
| `iri.find-first-of` | `purrdf-iri` | `parse::find_first_of` | `crates/iri/src/parse.rs:751` | `parse`, `intern_absoluteness` | iri-parse | I | — | covered | a u64 SWAR delimiter scan (zero-byte lanes of `word ^ splat(needle)`), exited by a trailing-zero count; measured in `parse::scan`, with the trailing-zero instruction as the proof (`bsf` / `tzcnt` / `rbit`+`clz` / `i64.ctz`) | SWAR on both builds: `i64.ctz` present, no vector work | swar v8·f0·r0·req✓ | swar v6·f0·r0·req✓ | swar v6·f0·r0·req✓ | swar v6·f0·r0·req✓ | swar v6·f0·r0·req✓ | swar v0·f0·r0·req✓ | swar v0·f0·r0·req✓ |
| `iri.scan-trivia` | `purrdf-iri` | `terminals::find_first_trivia` | `crates/iri/src/scan.rs:372` | `parse`, `tokenize`, `tokenize_values`, `update_reparse` | sparql-tokenizer | I | — | rewrite | the first byte that is not `WS` (exactly `is_ws`); sixteen-byte `as_chunks` lanes, each a `0x00`/`0xFF` byte from the class runs written as compares (a wrapping subtraction and an unsigned compare per run, derived at compile time from the `const [u8; 256]` class table, itself projected from the range table); a branch-free OR-fold of the lanes is the clean-chunk test, and the hit chunk reads its lanes as a little-endian `u128` whose trailing-zero count over 8 is the offset; the tail is answered from the class table. Formulation ladder, measured: `array::from_fn` lanes compiled to an out-of-line `try_from_fn` call per chunk (no compares packed); a `u16` bit-per-lane pack (OR-ing `u16::from(hit) << i` into a mask) packed for some classes and not others (x86_64 used `pmullw` word packing for the six- and eight-run classes, wasm32-simd128 stayed scalar for the three-run ones); the byte-lane form with an OR-fold is the one that packs every class on every vector configuration. Equivalence with the per-byte search is property-tested over every class boundary byte, non-ASCII input and lengths 0-70 plus longer runs. Called out of line by the SPARQL lexer's `skip_trivia`, one copy in `purrdf_iri` | +simd128: packed `i8x16` byte compares and a lane fold; baseline: the same lane loop as straight-line scalar code, same result | sse2 v10·f0·r0·req✓ | avx2 v22·f0·r0·req✓ | avx512 masks v18·f0·r0·req✓ | neon v8·f0·r0·req✓ | neon v8·f0·r0·req✓ | scalar v0·f0·r0 | simd128 v8·f0·r0·req✓ |
| `iri.scan-iri-body` | `purrdf-iri` | `terminals::find_first_iri_body_special` | `crates/iri/src/scan.rs:408` | `parse`, `tokenize`, `tokenize_values`, `update_reparse` | sparql-tokenizer | I | — | rewrite | the first `IRIREF`-forbidden byte (`is_iriref_forbidden_byte`, which holds the closing `>` and the `\` of a `UCHAR`); sixteen-byte `as_chunks` lanes, each a `0x00`/`0xFF` byte from the class runs written as compares (a wrapping subtraction and an unsigned compare per run, derived at compile time from the `const [u8; 256]` class table, itself projected from the range table); a branch-free OR-fold of the lanes is the clean-chunk test, and the hit chunk reads its lanes as a little-endian `u128` whose trailing-zero count over 8 is the offset; the tail is answered from the class table. Formulation ladder, measured: `array::from_fn` lanes compiled to an out-of-line `try_from_fn` call per chunk (no compares packed); a `u16` bit-per-lane pack (OR-ing `u16::from(hit) << i` into a mask) packed for some classes and not others (x86_64 used `pmullw` word packing for the six- and eight-run classes, wasm32-simd128 stayed scalar for the three-run ones); the byte-lane form with an OR-fold is the one that packs every class on every vector configuration. Equivalence with the per-byte search is property-tested over every class boundary byte, non-ASCII input and lengths 0-70 plus longer runs. Called out of line by `Lexer::lex_lt_or_iri`. With SVE in the build LLVM compares into predicate registers (`cmpeq`, `cmplo`) | +simd128: packed `i8x16` byte compares and a lane fold; baseline: the same lane loop as straight-line scalar code, same result | sse2 v17·f0·r0·req✓ | avx2 v16·f0·r0·req✓ | avx512 masks v8·f0·r0·req✓ | neon v14·f0·r0·req✓ | sve v11·f0·r0·req✓ | scalar v0·f0·r0 | simd128 v13·f0·r0·req✓ |
| `iri.scan-json-string` | `purrdf-iri` | `terminals::find_first_json_string_special` | `crates/iri/src/scan.rs:441` | `parse` | json-ordered | I | — | rewrite | the first `"`, `\` or C0 control of a JSON string body (`is_json_string_forbidden_byte`, RFC 8259 §7); sixteen-byte `as_chunks` lanes, each a `0x00`/`0xFF` byte from the class runs written as compares (a wrapping subtraction and an unsigned compare per run, derived at compile time from the `const [u8; 256]` class table, itself projected from the range table); a branch-free OR-fold of the lanes is the clean-chunk test, and the hit chunk reads its lanes as a little-endian `u128` whose trailing-zero count over 8 is the offset; the tail is answered from the class table. Formulation ladder, measured: `array::from_fn` lanes compiled to an out-of-line `try_from_fn` call per chunk (no compares packed); a `u16` bit-per-lane pack (OR-ing `u16::from(hit) << i` into a mask) packed for some classes and not others (x86_64 used `pmullw` word packing for the six- and eight-run classes, wasm32-simd128 stayed scalar for the three-run ones); the byte-lane form with an OR-fold is the one that packs every class on every vector configuration. Equivalence with the per-byte search is property-tested over every class boundary byte, non-ASCII input and lengths 0-70 plus longer runs. The scan `purrdf-json` reaches through `purrdf_core::terminals` | +simd128: packed `i8x16` byte compares and a lane fold; baseline: the same lane loop as straight-line scalar code, same result | sse2 v9·f0·r0·req✓ | avx2 v21·f0·r0·req✓ | avx512 masks v17·f0·r0·req✓ | neon v6·f0·r0·req✓ | neon v6·f0·r0·req✓ | scalar v0·f0·r0 | simd128 v7·f0·r0·req✓ |
| `iri.json-escape-controls` | `purrdf-iri` | `json_escape::find_first_json_controls_stop` | `crates/iri/src/json_escape.rs:117` | `parse` | — | I | — | covered | the clean-run scan of the workspace's one JSON string-escape law, `purrdf_iri::json_escape`, in its `Controls` spelling (`Minimal`'s `\"`, `\\`, `\n`, `\r`, `\t` and `\u00xx` for the other C0 controls, plus DEL and the C1 controls as `\u00xx`, so no raw control scalar is written). A `ByteClass` over the `find_first_json_string_special` class plus DEL and `0xC2`, the lead byte of every C1 control, compiled out of line as one kernel whichever writer calls it; a `0xC2` stop that leads a non-control (U+00A0 to U+00BF) is written as itself. The GTS proof and replication writers (`gts.mmr-json-escape`, `gts.replication-json-string`) run it, and their per-`char` escapers are kept as the oracles | +simd128 packed `i8x16` compares, baseline scalar lanes | sse2 v12·f0·r0·req✓ | avx2 v11·f0·r0·req✓ | avx512 masks v5·f0·r0·req✓ | neon v10·f0·r0·req✓ | neon v10·f0·r0·req✓ | scalar v0·f0·r0 | simd128 v9·f0·r0·req✓ |
| `iri.json-escape-ascii` | `purrdf-iri` | `json_escape::find_first_json_ascii_stop` | `crates/iri/src/json_escape.rs:108` | `parse` | — | I | — | covered | the clean-run scan of `purrdf_iri::json_escape` in its `Ascii` spelling (`ShortForms`, which adds `\b` and `\f`, plus DEL and every non-ASCII scalar as `\uxxxx`, an astral scalar as a UTF-16 surrogate pair: Python's `json.dumps` with `ensure_ascii=True`, byte for byte). A `ByteClass` over the `find_first_json_string_special` class plus DEL and every byte from `0x80`, compiled out of line as one kernel. `Minimal` and `ShortForms` stop at exactly the `find_first_json_string_special` class and run that scanner (`iri.scan-json-string`); the DSL stats emitter (`slice.dsl-stats-json-string`) runs this one | +simd128 packed `i8x16` compares, baseline scalar lanes | sse2 v12·f0·r0·req✓ | avx2 v24·f0·r0·req✓ | avx512 masks v18·f0·r0·req✓ | neon v8·f0·r0·req✓ | neon v8·f0·r0·req✓ | scalar v0·f0·r0 | simd128 v9·f0·r0·req✓ |
| `iri.scan-xml` | `purrdf-iri` | `terminals::find_first_xml_special` | `crates/iri/src/scan.rs:624` | `parse` | — | I | — | rewrite | the first byte XML 1.0 egress must replace (`&`, `<`, `>`, CR; `"`, TAB, LF in an attribute) or check (the C0 controls outside `Char`, and the lead byte `0xEF` of the block holding U+FFFE and U+FFFF); the class is derived from the `Char` range table plus the escaper's replacement set; sixteen-byte `as_chunks` lanes, each a `0x00`/`0xFF` byte from the class runs written as compares (a wrapping subtraction and an unsigned compare per run, derived at compile time from the `const [u8; 256]` class table, itself projected from the range table); a branch-free OR-fold of the lanes is the clean-chunk test, and the hit chunk reads its lanes as a little-endian `u128` whose trailing-zero count over 8 is the offset; the tail is answered from the class table. Formulation ladder, measured: `array::from_fn` lanes compiled to an out-of-line `try_from_fn` call per chunk (no compares packed); a `u16` bit-per-lane pack (OR-ing `u16::from(hit) << i` into a mask) packed for some classes and not others (x86_64 used `pmullw` word packing for the six- and eight-run classes, wasm32-simd128 stayed scalar for the three-run ones); the byte-lane form with an OR-fold is the one that packs every class on every vector configuration. Equivalence with the per-byte search is property-tested over every class boundary byte, non-ASCII input and lengths 0-70 plus longer runs. The scan the XML escaper of `purrdf-core` and the SPARQL results XML writer reach. With SVE in the build LLVM compares into predicate registers | +simd128: packed `i8x16` byte compares and a lane fold; baseline: the same lane loop as straight-line scalar code, same result | sse2 v12·f0·r0·req✓ | avx2 v11·f0·r0·req✓ | avx512 masks v6·f0·r0·req✓ | neon v10·f0·r0·req✓ | sve v8·f0·r0·req✓ | scalar v0·f0·r0 | simd128 v9·f0·r0·req✓ |
| `iri.validate-component` | `purrdf-iri` | `parse::validate_component` | `crates/iri/src/parse.rs:450` | `parse` | iri-parse | I | — | rewrite | an eight-byte clean-run precheck (`clean_prefix_len`) skips every leading byte of the next eight that is an ASCII member of the component's class, as compares: the `unreserved`/`sub-delims` runs derived from the `CLASS` table at compile time, plus one equality per admitted extra byte, as `0x00`/`0xFF` lanes whose `u64` trailing ones over 8 are the clean prefix. The per-`char` arm then handles the byte it stopped at exactly as before (a `%` escape, a non-ASCII lead, a refusal), so verdicts, errors and offsets are unchanged; the old per-byte validator is kept under test as the oracle over fixed-seed random components, every extra mask, both modes and both `iprivate` settings. The first formulation, `array::from_fn` lanes, packed on x86_64 but depended on inlining a generic call; the byte-lane form packs on every vector configuration | +simd128: packed `i8x16` byte compares for the precheck, plus the four-lane `i32x4.lt_u` range test of the `ucschar` arm; baseline scalar | sse2 v79·f0·r0·req✓ | avx2 v62·f0·r0·req✓ | avx512 masks v20·f0·r0·req✓ | neon v51·f0·r0·req✓ | sve v37·f0·r0·req✓ | scalar v0·f0·r0 | simd128 v57·f0·r0·req✓ |
| `iri.terminals-in-ranges` | `purrdf-iri` | `terminals::in_ranges` (behind every class predicate) | `crates/iri/src/terminals.rs:181` | `parse` | iri-parse | I | — | rewrite | every predicate answers a scalar below U+0100 by one load from a `const [u8; 256]` class table that `class_table` (a `const fn`) projects from the production's own range table, which stays the single source; only a scalar at or above U+0100 pays for the binary search. `lut_matches_ranges` checks every table against its range table on all 256 entries. A load is scalar by nature: the predicates now inline into every caller, so the entry is measured in `parse::scan`, which inlines `is_ipvfuture_address_char`, and records that symbol's counts, none of them the lookup | scalar on both builds | table v8·f0·r0 | table v6·f0·r0 | table v6·f0·r0 | table v6·f0·r0 | table v6·f0·r0 | table v0·f0·r0 | table v0·f0·r0 |
| `xsd.whitespace` | `purrdf-xsd` | `normalize_whitespace_replace`, `normalize_whitespace_collapse` | `crates/xsd/src/simple.rs:34` | `lexical` | xsd-lexical | I | — | rewrite | a sixteen-byte compare-formulated precheck returns the value unchanged (one copy, no per-byte loop) on the clean path, the common one; the public API returns `String`, so the clean path is `to_owned` rather than a borrow. `replace` finds the first `#x9`/`#xA`/`#xD` by `0x00`/`0xFF` lanes and an OR-fold and rewrites from there; `collapse` also tests leading, trailing and adjacent `#x20` (the space lanes against themselves shifted one lane, carried across chunks). Measured: when the hit extraction shared a body with the clean-chunk test, wasm32-simd128 kept `replace`'s compares scalar (v2), so the extraction moved to a cold out-of-line helper (`first_replaced_lane`), leaving the loop as compares and a reduction. Both are held to the previous byte scans and the per-`char` references over fixed-seed random values | +simd128: packed `i8x16` compares with `v128.any_true`; baseline scalar | replace sse2 v18·f0·r0·req✓<br>collapse sse2 v6·f0·r0·req✓ | replace avx2 v30·f0·r0·req✓<br>collapse avx2 v19·f0·r0·req✓ | replace avx512 masks v15·f0·r0·req✓<br>collapse avx512 masks v14·f0·r0·req✓ | replace neon v12·f0·r0·req✓<br>collapse neon v9·f0·r0·req✓ | replace neon v12·f0·r0·req✓<br>collapse neon v9·f0·r0·req✓ | replace scalar v0·f0·r0<br>collapse scalar v0·f0·r0 | replace simd128 v15·f0·r0·req✓<br>collapse simd128 v5·f0·r0·req✓ |
| `xsd.canonical-double` | `purrdf-xsd` | `numeric::canonical_double` | `crates/xsd/src/numeric.rs:380` | `lexical` | xsd-lexical | FI | the cost is `format!` of the shortest round-trip digits | leave | a formatting routine with no data-parallel loop; its bits are the canonical lexical form | scalar on both builds | v0·f0·r0 | v0·f0·r0 | v0·f0·r0 | v0·f0·r0 | v0·f0·r0 | v0·f0·r0 | v0·f0·r0 |
| `json.parser-string` | `purrdf-json` | `Parser::string` | `crates/json/src/parse.rs:249` | `ordered_json`, `ordered_json_alloc`, `ordered_json_corpus`, `jsonld_alloc` | json-ordered | I | — | rewrite | each clean run is crossed by one `terminals::find_first_json_string_special` call (`iri.scan-json-string`), reached through `purrdf_core::terminals`; only the byte it stops at takes the escape grammar. The parser borrows spans of a `&str`, so there is no `push_str` to batch and no UTF-8 check to repeat. The per-byte scan is kept under test as the oracle, compared by span, end offset and typed refusal | the scan: +simd128 packed `i8x16` compares, baseline scalar lanes; the call site: scalar on both builds | scan sse2 v9·f0·r0·req✓<br>call site v0·f0·r0 | scan avx2 v21·f0·r0·req✓<br>call site v0·f0·r0 | scan avx512 masks v17·f0·r0·req✓<br>call site v0·f0·r0 | scan neon v6·f0·r0·req✓<br>call site v0·f0·r0 | scan neon v6·f0·r0·req✓<br>call site v0·f0·r0 | scan scalar v0·f0·r0<br>call site v0·f0·r0 | scan simd128 v7·f0·r0·req✓<br>call site v0·f0·r0 |
| `json.parser-whitespace` | `purrdf-json` | `Parser::whitespace` | `crates/json/src/parse.rs:60` | `ordered_json`, `ordered_json_corpus` | json-ordered | I | runs between tokens are a few bytes long | leave | measured in `Parser::value`, which inlines it; the runs are too short for a chunked scan to pay its setup | scalar on both builds | v0·f0·r0 | v0·f0·r0 | v0·f0·r0 | v0·f0·r0 | v0·f0·r0 | v0·f0·r0 | v0·f0·r0 |
| `core.intern` | `purrdf-core` | `Interner::intern` | `crates/rdf-core/src/ir/builder.rs:285` | `intern_absoluteness`, `intern_content_id`, `ir_layout` | intern | I | the fixed-key ahash of the term is a loop-carried dependence; AES-NI hashing would need a build-wide target feature | leave | the count includes hashbrown's group probe (`pcmpeqb`, `pmovmskb`; `cmeq` on NEON). The fixed-key ahash rationale stands: ids are insertion-ordered and wasm32 has no entropy source | scalar on both wasm builds | v21·f0·r0 | v14·f0·r0 | v10·f0·r0 | v12·f0·r0 | v12·f0·r0 | v0·f0·r0 | v0·f0·r0 |
| `core.content-id-hex` | `purrdf-core` | `Blake3ContentId::from_hex`, `ContentDigest::from_hex`, `decode_hex_32` / `decode_hex_32_lower` | `crates/rdf-core/src/content_store.rs:111` | `intern_content_id` | intern | I | — | rewrite | the 64 digits are classified and valued sixteen at a time by comparisons only (`b - b'0' < 10`, `b - b'a' < 6`, with an OR of `0x20` folding `A-F` for the case-insensitive domain), each test a `0x00`/`0xFF` byte lane; a non-digit sets its lane of a sixteen-lane rejection accumulator OR-ed across the chunks and folded once, and only a clean input packs its nibble pairs. The lowercase domain is the same decode without the case fold, which refuses an uppercase digit exactly as the separate uppercase scan did, so every input is refused or accepted as before. Formulation ladder, measured: one 64-iteration loop with a scalar rejection byte compiled to a per-byte `cmov` loop on x86_64 (only the nibble pack vectorized, v18); the per-chunk lanes with a vertical accumulator pack the classification. The per-character decode and the uppercase scan are kept under test as the oracle over every ASCII byte at every position, every non-ASCII lead and continuation byte, and every wrong length | +simd128: packed `i8x16.lt_u`/`i8x16.gt_u` classification and `i8x16.shuffle` packing; baseline: straight-line scalar lanes | content-id sse2 v83·f0·r0·req✓<br>content-store sse2 v87·f0·r0·req✓ | content-id avx2 v60·f0·r0·req✓<br>content-store avx2 v64·f0·r0·req✓ | content-id avx512 masks v28·f0·r0·req✓<br>content-store avx512 masks v32·f0·r0·req✓ | content-id neon v58·f0·r0·req✓<br>content-store neon v62·f0·r0·req✓ | content-id neon v58·f0·r0·req✓<br>content-store neon v62·f0·r0·req✓ | content-id scalar v0·f0·r0<br>content-store scalar v0·f0·r0 | content-id simd128 v58·f0·r0·req✓<br>content-store simd128 v61·f0·r0·req✓ |
| `core.hex-lower` | `purrdf-core` | `hex::lower` | `crates/rdf-core/src/hex.rs:95` | `intern_content_id` | intern | I | — | rewrite | a buffer sized to `2 * len` up front, each byte written as its two digits; a nibble maps to its digit as `n + b'0'` plus a letter addend selected by a `0x00`/`0xFF` compare mask, so the loop has no formatting call and no branch, and packs into byte compares, adds and an interleave. The digit is computed by the compare rather than loaded from a sixteen-entry table, which keeps the loop in the compare-and-add shape the counts below show packed. The `write!` loop is kept under test as the oracle over all 256 bytes and every length to 70 | +simd128: `i8x16.gt_u` + `i8x16.add` + `i8x16.shuffle`; baseline: scalar | sse2 v36·f0·r0·req✓ | avx2 v27·f0·r0·req✓ | avx512 v25·f0·r0·req✓ | neon v27·f0·r0·req✓ | sve v23·f0·r0·req✓ | scalar v0·f0·r0 | simd128 v12·f0·r0·req✓ |
| `core.pack-dict-prefix` | `purrdf-core` | `pack::dict::encode_values`, `common_prefix_len` | `crates/rdf-core/src/ir/pack/dict.rs:401` | `pack_query` | pack_query | I | — | rewrite | `common_prefix_len` walks both records in eight-byte words: a zero XOR is eight shared bytes, and the first non-zero XOR ends the prefix at its trailing-zero count over 8 (little-endian words), with a byte tail. It is SWAR, not vector work: the proof is the 64-bit trailing-zero instruction in the inlined word loop (`bsfq` / `tzcntq` / `rbit`+`clz` / `i64.ctz`); on x86_64 hashbrown's group probes use 32-bit `bsfl`, so the 64-bit form is the prefix loop's alone. The vector counts are the id lookups' hashbrown probes (`pcmpeqb` + `pmovmskb`). The byte loop is kept under test as the oracle at every split point and bit of every length to 40 and over random strings of a small alphabet | SWAR on both builds (`i64.ctz`); the few +simd128 ops are hashbrown's | swar v66·f0·r0·req✓ | swar v58·f0·r0·req✓ | swar v24·f0·r0·req✓ | swar v23·f0·r0·req✓ | swar v18·f0·r0·req✓ | swar v0·f0·r0·req✓ | swar v3·f0·r0·req✓ |
| `core.pack-select-in-word` | `purrdf-core` | `pack::bits::select_in_word` | `crates/rdf-core/src/ir/pack/bits.rs:501` | `pack_bits` | pack_bits | I | — | rewrite | loop-free broadword select: per-byte popcounts by the SWAR bit count stopped at the byte, one multiply by `0x0101…01` into inclusive byte prefix sums, the target byte as the count of prefixes at most `r` (`((r·0x01…01 \| 0x80…80) - prefix) & 0x80…80`, counted by a second multiply), then a `const` 256×8 in-byte select table. Scalar by nature; the proof is the multiply and the absence of any loop, and on the builds with a popcount instruction LLVM recognizes the range check's total as one. The byte-skip-then-bit loop is kept under test as the oracle at every rank of 0, `!0`, every single bit and its complement, byte patterns, seeded random words thinned and thickened, and property-test words; the out-of-range refusal is tested with its in-range neighbour | scalar on both builds (`i64.mul`, `i64.popcnt`) | broadword v0·f0·r0·req✓ | broadword v0·f0·r0·req✓ | broadword v0·f0·r0·req✓ | broadword v2·f0·r0·req✓ | broadword v1·f0·r0·req✓ | broadword v0·f0·r0·req✓ | broadword v0·f0·r0·req✓ |
| `core.pack-rank1` | `purrdf-core` | `RankSelectDir::rank1` | `crates/rdf-core/src/ir/pack/bits.rs:568` | `pack_bits` | pack_bits | I | — | covered | two directory reads plus one in-word popcount: `popcntq` on x86-64-v3/v4, `cnt v.8b` + `addv` on NEON, SVE `cnt z.d` on neoverse-v1, `i64.popcnt` on wasm32. Baseline x86-64 has no POPCNT, so `count_ones` is the SWAR bit-count sequence there | `i64.popcnt` on both builds | swar popcount v0·f0·r0 | popcnt v0·f0·r0·req✓ | popcnt v0·f0·r0·req✓ | neon cnt v2·f0·r0·req✓ | sve cnt v1·f0·r0·req✓ | i64.popcnt v0·f0·r0·req✓ | i64.popcnt v0·f0·r0·req✓ |
| `core.pack-intvector-get` | `purrdf-core` | `IntVector::get` | `crates/rdf-core/src/ir/pack/bits.rs:237` | `pack_bits`, `pack_restore_alloc` | pack_bits | I | a two-word gather and shift for one value | leave | one random access; there is no second element to put in a lane | scalar on both builds | v0·f0·r0 | v0·f0·r0 | v0·f0·r0 | v0·f0·r0 | v0·f0·r0 | v0·f0·r0 | v0·f0·r0 |
| `core.dataset-scan-filter` | `purrdf-core` | `RdfDataset::quads_for_pattern_with_plan` (residual filter), `QuadMatches::next`, `QuadPatternCursor::next` | `crates/rdf-core/src/ir/dataset.rs:1249` | `ir_layout`, `mutable`, `shared_views`, `path_range` | ir_layout | I | — | rewrite | the pattern becomes one key and one mask word per field (a wildcard masks to zero, `GraphMatch::Default` keys the `0` niche of `None`), and a row matches when every `(word ^ key) & mask` is zero. A sequential candidate run (the SPOG slice or the whole-table fallback) is filtered eight rows at a time: each row a `0x0000_0000`/`0xFFFF_FFFF` lane, the lanes masked to their own bit and OR-folded, and the set bits yielded lowest first, which is row order; the tail and the permuted run test one row at a time with the same key. The owned cursor (`QuadPatternCursor`, from `quads_for_pattern_cursor`) holds the same `QuadKey` and shares the kernel: a sequential run is searched by `QuadKey::first_match` over the same eight-row `chunk_matches`, which is `#[inline(always)]` because with a plain `#[inline]` the second caller cut this crate's `QuadMatches::next` from 73 packed ops to 8 on x86_64; a permuted run tests each row with `QuadKey::matches`. Measured in `QuadMatches::next`, which each iterating crate inlines (every copy has the same counts), and in the cursor's `next` as a call site. The per-row `Option` filter and the per-row cursor loop are kept under test as the oracles, compared in order for every `(s?, p?, o?) × GraphMatch` combination (unused ids and an unknown graph included) over seeded datasets of every size to 40 and past several chunk multiples, both access shapes exercised | +simd128: `i32x4.eq` compares and an `i16x8.bitmask` row mask; baseline: straight-line scalar lanes | sse2 v73·f0·r0·req✓<br>cursor v97·f0·r0·req✓ | avx2 v47·f0·r0·req✓<br>cursor v63·f0·r0 | avx512 masks v18·f0·r0·req✓<br>cursor v34·f0·r0 | neon v58·f0·r0·req✓<br>cursor v76·f0·r0 | neon v58·f0·r0·req✓<br>cursor v76·f0·r0 | scalar v0·f0·r0<br>cursor v0·f0·r0 | simd128 v57·f0·r0·req✓<br>cursor v57·f0·r0 |
| `core.pack-triples-intersect` | `purrdf-core` | `pack::triples::partition_rows` (the `(?,p,o)` shorter-list probe), `DeltaListRef::next` | `crates/rdf-core/src/ir/pack/triples.rs:938` | `pack_index_compare`, `pack_query`, `pack_restore_alloc` | pack_index_compare | I | the `(?,p,o)` path decodes one FoQ list and keeps or drops each entry on a data-dependent test; the delta decode is a varint loop whose next offset depends on the current byte | leave | the `(?,p,o)` path no longer merges. Its list lengths were measured on the two comparison lanes: the WatDiv lane's frozen 10M-triple pack with its 20 instantiated queries, and the LUBM lane's generated data loaded as a pack with its 14 queries run without entailment. The path ran 1,520 times (1,500 WatDiv, 20 LUBM); one list was at least 32 times the other in 1,090 calls and at least 1,000 times in 1,065, only 430 were within 32 times (5 within 4 times), and the object list was the shorter in 1,518. A merge decoded 73,260,098 list entries for those calls, where the shorter lists hold 86,551. So the path reads both lengths from their count vectors, decodes only the shorter list and tests each entry in place: an object-index position is kept when `Sp` holds the predicate there, a predicate-index position when binary search finds the object in its `So` slice. The positions come out ascending, as the merge gave them; the merge is kept under test as the oracle over every predicate and object pair of a two-partition fixture skewed both ways, and `pack_index_compare` runs `pair_skewed_object_short` and `pair_skewed_predicate_short` beside `pair_dense` and `pair_sparse`. A galloping or block intersect would still decode the long list first; what remains has each step depending on the previous byte or on a data-dependent test, with no fixed-width independence to put in lanes | scalar on both builds | intersect v0·f0·r0<br>delta v0·f0·r0 | intersect v0·f0·r0<br>delta v0·f0·r0 | intersect v0·f0·r0<br>delta v0·f0·r0 | intersect v0·f0·r0<br>delta v0·f0·r0 | intersect v0·f0·r0<br>delta v0·f0·r0 | intersect v0·f0·r0<br>delta v0·f0·r0 | intersect v0·f0·r0<br>delta v0·f0·r0 |
| `core.canon-escape` | `purrdf-core` | `canon::write_iri_escaped`, `write_literal_escaped`, `find_first_literal_escape`; `iri_escape::find_first_candidate` | `crates/rdf-core/src/ir/canon.rs:2796` | `flat_view_canon` | rdf-escape | I | — | rewrite | clean-run batching with the RDFC-1.0 output bytes unchanged. The IRI writer delegates to `iri_escape::push_escaped`, now the one emission of the IRIREF egress law (the Turtle writer and the entailment surface use it too); the literal law (the canonical `ECHAR` set plus `\u00XX` for the other C0 controls and DEL, C1 raw) is neither `iri_escape`'s nor `xml_escape`'s, so it stays here as the one canonical-literal writer, now public, over a local all-ASCII class whose every stop is an escape. The kernel is `purrdf_iri::terminals::ByteClass`, the formulation of `iri.scan-json-string` exported for a class a writer owns: sixteen-byte `as_chunks` lanes, each a `0x00`/`0xFF` byte from the class runs written as compares, a branch-free OR-fold as the clean-chunk test, the hit chunk's lanes read as a little-endian `u128` whose trailing-zero count over 8 is the offset. The per-char writers are kept under test as the oracle over fixed-seed values holding every ASCII scalar and the whole `0xC2` block, and the W3C RDFC-1.0 fixtures pass unchanged | the scans: +simd128 packed `i8x16` compares, baseline scalar lanes; the drivers: scalar on both builds | iri scan sse2 v23·f0·r0·req✓<br>iri v0·f0·r0<br>literal scan sse2 v11·f0·r0·req✓<br>literal v0·f0·r0 | iri scan avx2 v22·f0·r0·req✓<br>iri v0·f0·r0<br>literal scan avx2 v23·f0·r0·req✓<br>literal v0·f0·r0 | iri scan avx512 masks v10·f0·r0·req✓<br>iri v0·f0·r0<br>literal scan avx512 masks v18·f0·r0·req✓<br>literal v0·f0·r0 | iri scan neon v20·f0·r0·req✓<br>iri v0·f0·r0<br>literal scan neon v8·f0·r0·req✓<br>literal v0·f0·r0 | iri scan sve v13·f0·r0·req✓<br>iri v0·f0·r0<br>literal scan neon v8·f0·r0·req✓<br>literal v0·f0·r0 | iri scan scalar v0·f0·r0<br>iri v0·f0·r0<br>literal scan scalar v0·f0·r0<br>literal v0·f0·r0 | iri scan simd128 v19·f0·r0·req✓<br>iri v0·f0·r0<br>literal scan simd128 v9·f0·r0·req✓<br>literal v0·f0·r0 |
| `core.purremb-f32-decode` | `purrdf-core` | `F32Scalars::check_finite`, `F32Scalars::decode_into`, `EffectiveF32Row::decode_into` (`first_non_finite_f32`), `F32Scalars::next` | `crates/rdf-core/src/ir/embedding/view.rs:909` | `purremb`, `purremb_alloc` | — | FI | — | rewrite | a bulk decode beside the lazy iterator: `chunks_exact(4)` little-endian words, sixteen per block, each lane a `0x0000_0000`/`0xFFFF_FFFF` word from a masked compare of the exponent field (the finiteness test on the bits) and a branch-free OR as the clean-block test; only a hit block is searched again, out of line and `#[cold]`, for its first scalar. `check_finite` returns the iterator's first `NonFiniteScalar` at the same column; `decode_into` appends exactly what `for v in row { out.push(v?) }` does, including the values before a failure; the normalized row divides the same way. The per-scalar iterator is unchanged and recorded beside the fold; whole-matrix verification uses `check_finite`. The drained iterator is kept under test as the oracle for every non-finite class at every position of every length to 40, with a later second non-finite, an advanced iterator, a nonzero first column and a trailing partial scalar | the fold: +simd128 `i32x4.eq` + `v128.any_true`, baseline scalar lanes; the iterator: scalar on both builds | fold sse2 v36·f0·r0·req✓<br>iterator v0·f0·r0 | fold avx2 v30·f0·r0·req✓<br>iterator v0·f0·r0 | fold avx512 v10·f0·r0·req✓<br>iterator v0·f0·r0 | fold neon v13·f0·r0·req✓<br>iterator v0·f0·r0 | fold neon v12·f0·r0·req✓<br>iterator v0·f0·r0 | fold scalar v0·f0·r0<br>iterator v0·f0·r0 | fold simd128 v48·f0·r0·req✓<br>iterator v0·f0·r0 |
| `core.purremb-norm` | `purrdf-core` | `norm_fold`, `deterministic_l2_norm` | `crates/rdf-core/src/ir/embedding/view.rs:4616` | `purremb`, `purremb_alloc` | — | FI | the scaled sum of squares is sequential by specification | leave | PURREMB §13.2 makes the written order normative (no FMA, sequential); measured in `L2F32Scalars::new` | scalar on both builds | v1·f0·r0 | v1·f0·r0 | v1·f0·r0 | v0·f0·r0 | v0·f0·r0 | v0·f0·r0 | v0·f0·r0 |
| `core.paged-map-quad` | `purrdf-core` | `paged::map_quad_to_global` | `crates/rdf-core/src/ir/paged/mod.rs:1148` | `paged_cross_page_bgp` | paged_cross_page_bgp | I | four independent table gathers per quad | leave | a gather over at most four columns; measured in `PagedDataset::from_provider`, the in-library symbol that inlines it (query-time copies are monomorphized in the caller) | scalar on both builds | v0·f0·r0 | v14·f0·r0 | v10·f0·r0 | v0·f0·r0 | v0·f0·r0 | v0·f0·r0 | v6·f0·r0 |
| `core.xsd-regex-scan` | `purrdf-core` | `xsd_regex::CompiledPattern::as_regex` → `regex::Regex::is_match` | `crates/rdf-core/src/xsd_regex/mod.rs:226` | `xsd_regex`, `regex_eval`, `pattern_validate` | regex_eval, shapes-pattern_validate, shex-pattern_validate | I | — | covered | the `regex` crate's literal prefilter: `regex-automata`'s `Memchr` calls memchr, which dispatches to `find_sse2` / `find_avx2` on x86_64 and is inlined on aarch64 and wasm32. An anchored pattern (`^Name[0-9]+$`, as in the benches) needs no prefilter and runs the scalar matching engines | +simd128: `i8x16.eq` + `i8x16.bitmask` in the prefilter; baseline: SWAR | sse2 v21·f0·r0·req✓<br>avx2 v24·f0·r0·req✓ | avx2 v24·f0·r0·req✓ | avx2 (avx512 masks) v10·f0·r0·req✓ | neon v18·f0·r0·req✓ | neon v18·f0·r0·req✓ | swar v0·f0·r0 | simd128 v18·f0·r0·req✓ |
| `core.sssom-tsv` | `purrdf-core` | `sssom::serialize_tsv` | `crates/rdf-core/src/sssom.rs:1119` | `sssom` | — | I | one `format!` per field and line into a `Vec<String>` | leave | allocation and formatting bound; no scan loop | scalar on both builds | v7·f0·r0 | v9·f0·r0 | v8·f0·r0 | v2·f0·r0 | v2·f0·r0 | v0·f0·r0 | v1·f0·r0 |
| `core.loss-json-escape` | `purrdf-core` | `loss::escape_json_into` (inlined into `loss::push_field`) | `crates/rdf-core/src/loss.rs:2064` | — | — | I | — | covered | a one-call writer over the workspace's one JSON string-escape law, `purrdf_iri::json_escape`, in its `ShortForms` spelling; the clean runs end at the law's scanner for that spelling (`iri.scan-json-string`), one out-of-line copy in `purrdf_iri`, measured there and repeated in this entry. The writer inlines into `push_field`, which is its measured call site; the law's own bench is `iri_json_escape_short_forms` in `crates/iri/benches/parse.rs` | the scan: +simd128 packed `i8x16` compares, baseline scalar lanes; the writer: scalar on both builds | scan sse2 v9·f0·r0·req✓<br>call site v0·f0·r0 | scan avx2 v21·f0·r0·req✓<br>call site v0·f0·r0 | scan avx512 masks v17·f0·r0·req✓<br>call site v0·f0·r0 | scan neon v6·f0·r0·req✓<br>call site v0·f0·r0 | scan neon v6·f0·r0·req✓<br>call site v0·f0·r0 | scan scalar v0·f0·r0<br>call site v0·f0·r0 | scan simd128 v7·f0·r0·req✓<br>call site v0·f0·r0 |
| `cli.pack-input` | `purrdf-cli` | `ImmutableInput::from_disk_path`, then `verify_pack` | `crates/cli/src/immutable.rs:153`, `crates/rdf-core/src/ir/pack/certify.rs:214` | `pack_input`, `pack_input_alloc`, `pack_restore_alloc` | — | I | acquisition is a sealed `memfd` snapshot or an owned read (system calls); verification re-derives the canonical digest | leave | the hashing inside `verify_pack` is the blake3/sha2 rows' kernels; the rest is decode and compare. `purrdf-cli` is not a wasm32 crate (its acquisition is file and `memfd` based, and it is absent from `make wasm`), so the row is measured in the `purrdf_core::…::verify_pack` it calls | scalar on both builds | v4·f0·r0 | v2·f0·r0 | v2·f0·r0 | v0·f0·r0 | v0·f0·r0 | v0·f0·r0 | v0·f0·r0 |
| `eval.regex-row-alloc` | `purrdf-sparql-eval` | `expr::eval_string_arg_expr` (per-row `REGEX` argument) | `crates/sparql-eval/src/expr.rs:3905` | `regex_eval` | regex_eval | I | each row's text argument is materialized into an owned `String` before `is_match` | leave | the cost is the allocation, not a loop LLVM could widen | +simd128 packs a few `i8x16` lane ops in the symbol; no scan loop on either build | v15·f0·r0 | v33·f0·r0 | v21·f0·r0 | v12·f0·r0 | v10·f0·r0 | v0·f0·r0 | v4·f0·r0 |
| `eval.solution-compatible` | `purrdf-sparql-eval` | `solution::compatible`, `bgp::bind_row` | `crates/sparql-eval/src/solution.rs:372` | `solution_row`, `exists_decorrelation`, `lateral_service`, `lateral_substitution`, `query_eval`, `relation_attestation_alloc` | solution_row | I | `Option` equality over at most four shared columns | leave | a gather over at most four columns, exited on the first mismatch | scalar on both builds | compatible v0·f0·r0<br>bind_row v0·f0·r0 | compatible v0·f0·r0<br>bind_row v0·f0·r0 | compatible v0·f0·r0<br>bind_row v0·f0·r0 | compatible v0·f0·r0<br>bind_row v0·f0·r0 | compatible v0·f0·r0<br>bind_row v0·f0·r0 | compatible v0·f0·r0<br>bind_row v0·f0·r0 | compatible v0·f0·r0<br>bind_row v0·f0·r0 |
| `eval.bgp-cost-planner` | `purrdf-sparql-eval` | `bgp::cost_order_dp` | `crates/sparql-eval/src/bgp.rs:778` | `cost_based_bgp_planner`, `query_eval` | — | FR | f64 plan arithmetic over a handful of patterns, once per query | leave | not a loop over data; the count is bitset and index bookkeeping in the dynamic program | integer lane ops in the bookkeeping under +simd128; the cost arithmetic is scalar on both builds | v26·f0·r0 | v40·f0·r0 | v40·f0·r0 | v17·f0·r0 | v5·f0·r0 | v0·f0·r0 | v15·f0·r0 |
| `eval.bgp-join-probe` | `purrdf-sparql-eval` | `bgp::eval_bgp` inner loop | `crates/sparql-eval/src/bgp.rs:99` | `query_eval`, `construct_builder`, `cost_based_bgp_planner`, `exists_decorrelation`, `governed_eval`, `paged_cross_page_bgp`, `path_range`, `prepared_admission`, `prepared_reuse`, `relation_attestation_alloc`, `query_engine_reuse` | bgp-join-probe | I | enum dispatch per candidate quad and a `SmallVec` binding per row | leave | the per-candidate work is a dependent chain of dispatch and binding, not a lane-parallel loop | scalar on both builds | v14·f0·r0 | v60·f0·r0 | v57·f0·r0 | v10·f0·r0 | v10·f0·r0 | v0·f0·r0 | v10·f0·r0 |
| `eval.aggregate-fold` | `purrdf-sparql-eval` | `NumericFold::step_xsd`, `NumericFold::combine_owned` (behind `fold_numeric`/`NumericSummary`), `numeric_add`, `BigInt::add_i128`, `BigInt::add_assign` | `crates/sparql-eval/src/modifier.rs:1939` | `query_eval` | sparql-aggregate | FI | one `TermValue` per row through the promotion tower (`BigInt` integer tier, `i128`-mantissa decimal tier, IEEE float/double tier) with poisoning; no slice of doubles exists to fill a lane | leave | SPARQL 1.1 §18.5.1.3 defines `Sum` as the chain `op:numeric-add(S1, Sum(S2..n))` over `ToList`, whose order is free but whose shape is a chain. Regime 2 makes the `xsd:double` bits depend on the target, and regime 3's lane tree gives bits no chain gives: over {−3, −2^53, −1, −0.7} every chain order answers −9007199254740996 and the pairwise tree −9007199254740998. So neither regime is admissible, and the same holds for chunking a group across threads: the parallel fold merges chunk partials only where the merge provably equals the chain (pure-integer `BigInt` partials, or decimal and duration partials whose magnitude bound rules out every `i128`-mantissa overflow) and replays every row from the first float/double operand on in source order, so the parallel and sequential answers are the same bits. The integer tier is an exact carry chain and the decimal tier a scale-aligned `i128` add, one value per row. The integer-to-float/double promotion is `BigInt::to_f64`/`to_f32`: one correctly rounded integer cast of the top 64 bits with a sticky bit, scaled by an exact power of two, with no multiply-add, so no configuration fuses anything (the earlier per-limb `mul_add` Horner conversion contracted to hardware FMA and could misround by an ulp); the `x86_64` vector ops are `xorps` sign flips | scalar on both builds | step v0·f0·r0<br>combine v0·f0·r0<br>numeric_add v0·f0·r0<br>add_i128 v0·f0·r0<br>add_assign v0·f0·r0 | step v0·f0·r0<br>combine v0·f0·r0<br>numeric_add v0·f0·r0<br>add_i128 v0·f0·r0<br>add_assign v0·f0·r0 | step v0·f0·r0<br>combine v0·f0·r0<br>numeric_add v0·f0·r0<br>add_i128 v0·f0·r0<br>add_assign v0·f0·r0 | step v0·f0·r0<br>combine v0·f0·r0<br>numeric_add v0·f0·r0<br>add_i128 v0·f0·r0<br>add_assign v0·f0·r0 | step v0·f0·r0<br>combine v0·f0·r0<br>numeric_add v0·f0·r0<br>add_i128 v0·f0·r0<br>add_assign v0·f0·r0 | step v0·f0·r0<br>combine v0·f0·r0<br>numeric_add v0·f0·r0<br>add_i128 v0·f0·r0<br>add_assign v0·f0·r0 | step v0·f0·r0<br>combine v0·f0·r0<br>numeric_add v0·f0·r0<br>add_i128 v0·f0·r0<br>add_assign v0·f0·r0 |
| `datalog.relation-contains` | `purrdf-datalog` | `Relation::contains` (tail probe `Tail::contains`, inlined) | `crates/datalog/src/store.rs:726` | `seminaive` | datalog-seminaive | I | — | rewrite | the mutable tail is held as structure-of-arrays columns (`Tail`: subjects, objects, row ids, one row per index) and the duplicate probe OR-folds `(s == subject) & (o == object)` over the two key columns with no exit on a hit. The same branch-free fold over the `(TermId, TermId, RowId)` tuples was measured first: v0 on x86_64, an unrolled scalar `xor`/`sete`/`orb` loop, because the tuple layout puts consecutive keys three words apart; over the columns it packs on every SIMD configuration. The cursor legs read the tail through `Tail::row` and `Tail::iter` in insertion order, and sealing drains it with `Tail::take_rows` in that order, so the batch built from it is unchanged. The galloping batch probe is unchanged and is the rest of the function. The tuple scan is kept under test as the oracle, compared on seeded tails of every length 0 to 64 (keys drawn from a small id space so they collide), with a hit at every position, a lone hit among one-column near misses at every position, swapped and absent keys, and the columns' round trip in insertion order | +simd128: `i32x4.eq` compares answered by `v128.any_true`; baseline: a scalar loop with no exit | sse2 v13·f0·r0·req✓ | avx2 v34·f0·r0·req✓ | avx512 masks v10·f0·r0·req✓ | neon v43·f0·r0·req✓ | sve v15·f0·r0·req✓ | scalar v0·f0·r0 | simd128 v7·f0·r0·req✓ |
| `datalog.gallop-leapfrog` | `purrdf-datalog` | `gallop_lower_bound` (in `Batch::subject_run`), `LeapfrogValueCursor::seek` | `crates/datalog/src/store.rs:341` | `seminaive` | datalog-seminaive | I | exponential then binary search; each probe depends on the previous comparison | leave | loop-carried search | scalar on both builds | gallop v0·f0·r0<br>leapfrog v0·f0·r0 | gallop v0·f0·r0<br>leapfrog v0·f0·r0 | gallop v0·f0·r0<br>leapfrog v0·f0·r0 | gallop v0·f0·r0<br>leapfrog v0·f0·r0 | gallop v0·f0·r0<br>leapfrog v0·f0·r0 | gallop v0·f0·r0<br>leapfrog v0·f0·r0 | gallop v0·f0·r0<br>leapfrog v0·f0·r0 |
| `datalog.chase` | `purrdf-datalog` | `chase::chase_until` | `crates/datalog/src/chase.rs:1325` | `chase` | entail-chase | I | a fixpoint over relation probes; each round depends on the last | leave | loop-carried fixpoint; the probes are the relation rows above | scalar on both builds | v1·f0·r0 | v11·f0·r0 | v3·f0·r0 | v0·f0·r0 | v0·f0·r0 | v0·f0·r0 | v0·f0·r0 |
| `entail.escape` | `purrdf-entail` | `engine::write_iri_escaped`, `write_literal_escaped` | `crates/entail/src/engine.rs:1395` | `chase` | — | I | — | rewrite | both writers delegate: the IRI one to `purrdf_core::iri_escape::push_escaped`, the literal one to `purrdf_core::ir::canon::write_literal_escaped`, the canonical N-Quads writer this surface is defined to match, so the laws are no longer transcribed here and the scans are the ones `core.canon-escape` measures (repeated in this entry). `write_literal_escaped` is measured in `write_surface`, which inlines it. The per-char writers are kept under test as the oracle over fixed-seed values | the scans: +simd128 packed `i8x16` compares, baseline scalar lanes; the drivers: scalar on both builds | iri scan sse2 v23·f0·r0·req✓<br>iri v0·f0·r0<br>literal scan sse2 v11·f0·r0·req✓<br>literal (in write_surface) v0·f0·r0 | iri scan avx2 v22·f0·r0·req✓<br>iri v0·f0·r0<br>literal scan avx2 v23·f0·r0·req✓<br>literal (in write_surface) v4·f0·r0 | iri scan avx512 masks v10·f0·r0·req✓<br>iri v0·f0·r0<br>literal scan avx512 masks v18·f0·r0·req✓<br>literal (in write_surface) v3·f0·r0 | iri scan neon v20·f0·r0·req✓<br>iri v0·f0·r0<br>literal scan neon v8·f0·r0·req✓<br>literal (in write_surface) v0·f0·r0 | iri scan sve v13·f0·r0·req✓<br>iri v0·f0·r0<br>literal scan neon v8·f0·r0·req✓<br>literal (in write_surface) v0·f0·r0 | iri scan scalar v0·f0·r0<br>iri v0·f0·r0<br>literal scan scalar v0·f0·r0<br>literal (in write_surface) v0·f0·r0 | iri scan simd128 v19·f0·r0·req✓<br>iri v0·f0·r0<br>literal scan simd128 v9·f0·r0·req✓<br>literal (in write_surface) v0·f0·r0 |
| `entail.classify` | `purrdf-entail` | `Subsumptions::decide`, `owl_dl::saturate::saturate` | `crates/entail/src/reasoner/classify.rs:97` | `classify`, `consistency`, `schema_scan` | entail-classify | I | ordered-map and work-queue pointer chasing over concept ids | leave | no contiguous data-parallel loop | scalar on both builds | decide v2·f0·r0<br>saturate v0·f0·r0 | decide v9·f0·r0<br>saturate v0·f0·r0 | decide v0·f0·r0<br>saturate v0·f0·r0 | decide v0·f0·r0<br>saturate v0·f0·r0 | decide v0·f0·r0<br>saturate v0·f0·r0 | decide v0·f0·r0<br>saturate v0·f0·r0 | decide v1·f0·r0<br>saturate v0·f0·r0 |
| `shapes.frontier-dedup` | `purrdf-shapes` | `FrontierDedup::insert` (linear stage `linear_contains`, inlined) | `crates/shapes/src/path.rs:158` | `pattern_lookup` | shapes-pattern_lookup | I | — | rewrite | the linear stage (fewer than `LINEAR_MAX` = 16 admitted ids) tests every id with no exit on a hit, four at a time: each chunk's compares are `0x0000_0000`/`0xFFFF_FFFF` `u32` lanes OR-ed into a lane accumulator that is folded once after the last chunk, and the at most three ids after the last whole chunk are tested one at a time. Four lanes rather than eight because the stage never holds more than fifteen ids, so a wider chunk would leave most frontiers in the one-at-a-time tail; on x86_64 the compiled loop takes one chunk per iteration as a `pcmpeqd` + `por`, folded by `movmskps`. The count also covers the promotion into the hashed set and its call into the set's own insert. The slice's short-circuiting `contains` and the dedup it sat in are kept under test as the oracles: every frontier size 0 to 20 with a hit at every position and misses, seeded frontiers with repeats at every size 0 to 16, seeded id streams across the promotion and a `clear`, and the boundary itself at 15, 16 and 17 admitted ids | +simd128: `i32x4.eq` compares answered by `v128.any_true`; baseline: straight-line scalar compares with no exit | sse2 v72·f0·r0·req✓ | avx2 v173·f0·r0·req✓ | avx512 masks v50·f0·r0·req✓ | neon v138·f0·r0·req✓ | sve v64·f0·r0·req✓ | scalar v0·f0·r0 | simd128 v31·f0·r0·req✓ |
| `shapes.path-eval` | `purrdf-shapes` | `path::eval_planned_ids_from_id` | `crates/shapes/src/path.rs:220` | `pattern_lookup`, `validate`, `shacl_product_alloc`, `shacl_product_reuse`, `shared_views` | shapes-pattern_lookup | I | the walk calls `FrontierDedup::insert` per reached id | leave | the walk is predicate-slot reads, pattern cursors and recursion over the lowered path, with no fixed-width independent work in it; its one vector-shaped step, the frontier membership probe, is `FrontierDedup::insert`, which stays out of line and is the `shapes.frontier-dedup` row (four ids per packed compare in its linear stage). This symbol is the walk alone, and it compiles to no vector work on any configuration | scalar on both builds | v0·f0·r0 | v0·f0·r0 | v0·f0·r0 | v0·f0·r0 | v0·f0·r0 | v0·f0·r0 | v0·f0·r0 |
| `shapes.finish-report` | `purrdf-shapes` | `engine::finish_report` | `crates/shapes/src/engine.rs:1095` | `validate`, `canonical_sort`, `shacl_product_alloc`, `shacl_product_reuse` | — | I | a sort and assembly of result records | leave | allocation and comparison sort | scalar on both builds | v0·f0·r0 | v0·f0·r0 | v0·f0·r0 | v0·f0·r0 | v0·f0·r0 | v0·f0·r0 | v0·f0·r0 |
| `shapes.pydantic-emit` | `purrdf-shapes` | `pydantic::emit_pydantic` | `crates/shapes/src/pydantic.rs:339` | `pydantic_package` | — | I | string emission from a compiled schema | leave | formatting and allocation bound | scalar on both builds | v13·f0·r0 | v10·f0·r0 | v5·f0·r0 | v6·f0·r0 | v4·f0·r0 | v0·f0·r0 | v2·f0·r0 |
| `shapes.schema-compile` | `purrdf-shapes` | `json_schema::compile_schema` | `crates/shapes/src/json_schema.rs:980` | `schema_surface` | — | I | a graph walk building JSON values | leave | allocation and map-building bound | scalar on both builds | v0·f0·r0 | v0·f0·r0 | v0·f0·r0 | v0·f0·r0 | v0·f0·r0 | v0·f0·r0 | v0·f0·r0 |
| `shex.char-count` | `purrdf-shex` | `str::chars().count()` in the length facets | `crates/shex/src/validate/node.rs:179` | `pattern_validate` | shex-pattern_validate | I | — | covered | std's counter: `chars().count()` calls core's out-of-line `do_count_chars`, a word-at-a-time count compiled into the standard library, so this build's asm holds the call and not the body. Measured in `check_node_constraint`, with the call as the proof | the same call on both builds | std count v37·f0·r0·req✓ | std count v23·f0·r0·req✓ | std count v15·f0·r0·req✓ | std count v19·f0·r0·req✓ | std count v19·f0·r0·req✓ | std count v0·f0·r0·req✓ | std count v0·f0·r0·req✓ |
| `shex.matcher` | `purrdf-shex` | `matcher::search` | `crates/shex/src/validate/matcher.rs:329` | `prepared_validate` | shex-pattern_validate | I | backtracking search over triple-expression partitions | leave | control flow, not data parallelism | scalar on both builds | v0·f0·r0 | v0·f0·r0 | v0·f0·r0 | v0·f0·r0 | v0·f0·r0 | v0·f0·r0 | v0·f0·r0 |
| `results.xml-escape` | `purrdf-sparql-results` | `xml_escape::push_into` (via `xml_escape_text`), `xml_escape::escape` | `crates/rdf-core/src/xml_escape.rs:139` | `graph_serialize` | — | I | — | rewrite | one `terminals::find_first_xml_special` pass (`iri.scan-xml`) validates and finds references at once: it stops only where a scalar outside `Char` or a scalar needing a reference can begin, the scalar there is checked and classified, and the offsets of the first 32 references are held on the stack, so emission copies the runs between them with no second scan; only a value with more references than that is scanned again from the first one not held, for emission only. Validity is still decided before a byte is written, so a refusal emits nothing into a sink that cannot rewind, and nothing is allocated. The two-pass escaper is kept under test as the oracle over fixed-seed values in both contexts, dense enough to pass the held references, with refusals compared by offset and scalar. `xml_escape_text` is out of line on x86_64 and aarch64 and inlined into `write_term` on wasm32 | the scan: +simd128 packed `i8x16` compares, baseline scalar lanes; the drivers: scalar on both builds | scan sse2 v12·f0·r0·req✓<br>push_into v0·f0·r0<br>writer v0·f0·r0 | scan avx2 v11·f0·r0·req✓<br>push_into v0·f0·r0<br>writer v0·f0·r0 | scan avx512 masks v6·f0·r0·req✓<br>push_into v0·f0·r0<br>writer v0·f0·r0 | scan neon v10·f0·r0·req✓<br>push_into v0·f0·r0<br>writer v0·f0·r0 | scan sve v8·f0·r0·req✓<br>push_into v0·f0·r0<br>writer v0·f0·r0 | scan scalar v0·f0·r0<br>push_into v0·f0·r0<br>writer v0·f0·r0 | scan simd128 v9·f0·r0·req✓<br>push_into v0·f0·r0<br>writer v0·f0·r0 |
| `results.csv-field` | `purrdf-sparql-results` | `csv::push_field`, `find_first_quoting` | `crates/sparql-results/src/csv.rs:187` | `graph_serialize` | — | I | — | rewrite | one scan: the first `"`, `,`, LF or CR decides the field is quoted, and the same scan continues from it, stopping at each later member so a `"` is doubled and copying every run between stops whole. The kernel is `purrdf_iri::terminals::ByteClass`, the formulation of `iri.scan-json-string` exported for a class a writer owns: sixteen-byte `as_chunks` lanes, each a `0x00`/`0xFF` byte from the class runs written as compares, a branch-free OR-fold as the clean-chunk test, the hit chunk's lanes read as a little-endian `u128` whose trailing-zero count over 8 is the offset. The two-pass writer is kept under test as the oracle over fixed-seed values | the scan: +simd128 packed `i8x16.eq`, baseline scalar lanes; the driver: scalar on both builds | scan sse2 v10·f0·r0·req✓<br>call site v0·f0·r0 | scan avx2 v22·f0·r0·req✓<br>call site v0·f0·r0 | scan avx512 masks v18·f0·r0·req✓<br>call site v0·f0·r0 | scan neon v8·f0·r0·req✓<br>call site v0·f0·r0 | scan neon v8·f0·r0·req✓<br>call site v0·f0·r0 | scan scalar v0·f0·r0<br>call site v0·f0·r0 | scan simd128 v9·f0·r0·req✓<br>call site v0·f0·r0 |
| `results.json-escape` | `purrdf-sparql-results` | `json::json_escape_body` (a call to the law's `escape_body`) | `crates/sparql-results/src/json.rs:362` | `graph_serialize` | — | I | — | rewrite | measured before the rewrite as not covered: `position` over a byte predicate compiled to a byte-at-a-time compare loop on every configuration. `json_escape_body` is now a one-line call to `purrdf_iri::json_escape::escape_body` in the `Minimal` spelling of the workspace's one JSON string-escape law, `purrdf_iri::json_escape`, so its clean run ends at `terminals::find_first_json_string_special` (`iri.scan-json-string`), whose class is exactly the old trigger predicate (checked on all 256 bytes); the scanner is one out-of-line copy in `purrdf_iri`, measured there and repeated in this entry. `json_escape_body` inlines into its callers, so the call site is measured in `escape_body` instantiated for this writer's `TextSink` closure. The per-`char` escaper is the oracle over fixed-seed values | the scan: +simd128 packed `i8x16` compares, baseline scalar lanes; the driver: scalar on both builds | scan sse2 v9·f0·r0·req✓<br>call site v0·f0·r0 | scan avx2 v21·f0·r0·req✓<br>call site v0·f0·r0 | scan avx512 masks v17·f0·r0·req✓<br>call site v0·f0·r0 | scan neon v6·f0·r0·req✓<br>call site v0·f0·r0 | scan neon v6·f0·r0·req✓<br>call site v0·f0·r0 | scan scalar v0·f0·r0<br>call site v0·f0·r0 | scan simd128 v7·f0·r0·req✓<br>call site v0·f0·r0 |
| `markdown.lines` | `purrdf-markdown` | `dialect::markdown::lines`, `find_line_feed` | `crates/markdown/src/dialect/markdown.rs:106` | `slicer` | — | I | — | rewrite | a local chunked `\n` scan (`find_line_feed`), a one-run `ByteClass` reached through `purrdf_core::terminals`, so there is no new dependency. Formulation ladder, measured: the multi-run OR-fold vectorized eight lanes of sixteen on aarch64 and tested the rest byte by byte; an `eor`-then-minimum fold vectorized but lowered to a `pminub` shuffle ladder on x86_64; reading the hit lanes as a `u128` spilled them and scalarized the test on both. The one-run kernel therefore tests the lanes' maximum and re-tests the hit chunk's bytes for the offset, which is `pcmpeqb` + `pmovmskb`, `cmeq .16b` + `addp` and `i8x16.eq` + `v128.any_true`. `lines` is inlined through `read` into `analyze`. The per-byte split is kept under test as the oracle | the scan: +simd128 `i8x16.eq` + `v128.any_true`, baseline scalar lanes; the call site: scalar on both builds | scan sse2 v2·f0·r0·req✓<br>call site (in analyze) v0·f0·r0 | scan avx2 v2·f0·r0·req✓<br>call site (in analyze) v0·f0·r0 | scan avx512 masks v1·f0·r0·req✓<br>call site (in analyze) v0·f0·r0 | scan neon v2·f0·r0·req✓<br>call site (in analyze) v0·f0·r0 | scan neon v2·f0·r0·req✓<br>call site (in analyze) v0·f0·r0 | scan scalar v0·f0·r0<br>call site (in analyze) v0·f0·r0 | scan simd128 v2·f0·r0·req✓<br>call site (in analyze) v4·f0·r0 |
| `distance.exact.dot` | `purrdf-core` | `exact::dot` (the `Exact` dot fold, in the `dispatch` batch and pair kernels) | `crates/rdf-core/src/distance/exact.rs:53` | `distance`, `knn_relation`, `recall` | — | FR | — | regime-3 | the fixed-lane exact law of §3.1: sixteen binary64 lanes over `as_chunks::<16>` rebuilt with `array::from_fn`, the pairwise tree, a sequential tail, every product a named local. Measured in the batch kernels the kNN scan and the HNSW beam call (`distances` over f64×f64, `distances_indexed` over f64×f32 and f32×f32), each in its portable compilation and, on x86_64, its `#[target_feature(enable = "avx2")]` and `#[target_feature(enable = "avx512f")]` ones, AVX-512F selected first (by the rule the reassociated `avx512f` path uses: AVX-512F, AVX2 and FMA reported). The AVX-512F compilation is kept on its count, not on an argument: its sixteen lanes are two `zmm` registers (required as `%zmm`) where AVX2 uses four `ymm`, so each sixteen-element block issues two packed multiplies and two adds where AVX2 issues four of each, and every kernel is shorter than its AVX2 compilation (the f64×f64 batch on the baseline build is v117 against v157). It stays out of line and `zmm` in an x86-64-v3 build; in an x86-64-v4 build the callers already have AVX-512F, so it is inlined into the portable symbol, which is required as `zmm` there. No exact path carries FMA; the order is proven by the scalar reference-model test, not by these counts (§2.4). The reassociated fold is a separate function under its own contract (§3.2) | +simd128: the lanes pack into `f64x2.add` / `f64x2.mul`, no `relaxed_` op; baseline: scalar `f64.add` / `f64.mul`, the same order | batch f64×f64 v169·f0·r0·req✓<br>batch f64×f64 avx2 v157·f0·r0·req✓<br>batch f64×f64 avx512f v117·f0·r0·req✓<br>indexed f64×f32 v235·f0·r0·req✓<br>indexed f64×f32 avx2 v149·f0·r0·req✓<br>indexed f64×f32 avx512f v137·f0·r0·req✓<br>indexed f32×f32 v299·f0·r0·req✓<br>indexed f32×f32 avx2 v173·f0·r0·req✓<br>indexed f32×f32 avx512f v157·f0·r0·req✓ | batch f64×f64 avx512f v133·f0·r0·req✓<br>batch f64×f64 v157·f0·r0·req✓<br>indexed f64×f32 avx512f v161·f0·r0·req✓<br>indexed f64×f32 v197·f0·r0·req✓<br>indexed f32×f32 avx512f v189·f0·r0·req✓<br>indexed f32×f32 v237·f0·r0·req✓ | batch f64×f64 v133·f0·r0·req✓<br>indexed f64×f32 v161·f0·r0·req✓<br>indexed f32×f32 v189·f0·r0·req✓ | batch f64×f64 v160·f0·r0·req✓<br>indexed f64×f32 v208·f0·r0·req✓<br>indexed f32×f32 v256·f0·r0·req✓ | batch f64×f64 v167·f0·r0·req✓<br>indexed f64×f32 v215·f0·r0·req✓<br>indexed f32×f32 v270·f0·r0·req✓ | batch f64×f64 v0·f0·r0<br>indexed f64×f32 v0·f0·r0<br>indexed f32×f32 v0·f0·r0 | batch f64×f64 v149·f0·r0·req✓<br>indexed f64×f32 v183·f0·r0·req✓<br>indexed f32×f32 v231·f0·r0·req✓ |
| `distance.exact.sqeuclid` | `purrdf-core` | `exact::squared_euclidean_bounded` (the `Exact` squared-Euclidean fold, in the `dispatch` bounded and pair kernels) | `crates/rdf-core/src/distance/exact.rs:92` | `build`, `distance`, `knn_relation`, `recall` | — | FR | — | regime-3 | the same law; the bounded form checkpoints every four chunks (64 elements), tests non-finite before the bound, and the full form is the bounded form with an infinite bound. Measured in the pair kernels, portable, AVX2 and AVX-512F compilations (the AVX-512F one two `zmm` lanes, with two packed subtracts per sixteen-element block where AVX2 issues four, as in `distance.exact.dot`): `distance_bounded` over f64×f64 and f32×f32, which HNSW's neighbour selection and connectivity repair call, and `distance` over f64×f64 and f32×f32, the resolved-handle pair path (`Resolved::<Exact>::distance` → `Exact::distance_on` → `dispatch::distance`) that `HnswIndex::row_distance` and a row-seeded search reach through `VectorMatrix::distance`, with the difference (`subpd` / `vsubpd` / `fsub .2d` / `f64x2.sub`) required as the squared-Euclidean proof. No public entry computes a pair without a resolved handle, and every one (`Kernel::distance`, `VectorMatrix::distance_from_query`) is generic, so a pair is instantiated only at the widths a caller names: the audited graph compiles no f64×f32 pair, and the mixed-width fold is measured where the library compiles it, the HNSW beam's `distances_indexed` over f64×f32 (`distance.exact.dot`) | +simd128: `f64x2.sub` / `f64x2.mul` / `f64x2.add` lanes, no `relaxed_` op; baseline: scalar, the same order | bounded f64×f64 v169·f0·r0·req✓<br>bounded f64×f64 avx2 v157·f0·r0·req✓<br>bounded f64×f64 avx512f v117·f0·r0·req✓<br>bounded f32×f32 v201·f0·r0·req✓<br>bounded f32×f32 avx2 v125·f0·r0·req✓<br>bounded f32×f32 avx512f v89·f0·r0·req✓<br>pair f64×f64 v169·f0·r0·req✓<br>pair f64×f64 avx2 v157·f0·r0·req✓<br>pair f64×f64 avx512f v117·f0·r0·req✓<br>pair f32×f32 v201·f0·r0·req✓<br>pair f32×f32 avx2 v125·f0·r0·req✓<br>pair f32×f32 avx512f v89·f0·r0·req✓ | bounded f64×f64 avx512f v133·f0·r0·req✓<br>bounded f64×f64 v157·f0·r0·req✓<br>bounded f32×f32 avx512f v89·f0·r0·req✓<br>bounded f32×f32 v125·f0·r0·req✓<br>pair f64×f64 avx512f v133·f0·r0·req✓<br>pair f64×f64 v157·f0·r0·req✓<br>pair f32×f32 avx512f v89·f0·r0·req✓<br>pair f32×f32 v125·f0·r0·req✓ | bounded f64×f64 v133·f0·r0·req✓<br>bounded f32×f32 v89·f0·r0·req✓<br>pair f64×f64 v133·f0·r0·req✓<br>pair f32×f32 v89·f0·r0·req✓ | bounded f64×f64 v160·f0·r0·req✓<br>bounded f32×f32 v192·f0·r0·req✓<br>pair f64×f64 v160·f0·r0·req✓<br>pair f32×f32 v192·f0·r0·req✓ | bounded f64×f64 v167·f0·r0·req✓<br>bounded f32×f32 v206·f0·r0·req✓<br>pair f64×f64 v167·f0·r0·req✓<br>pair f32×f32 v206·f0·r0·req✓ | bounded f64×f64 v0·f0·r0<br>bounded f32×f32 v0·f0·r0<br>pair f64×f64 v0·f0·r0<br>pair f32×f32 v0·f0·r0 | bounded f64×f64 v149·f0·r0·req✓<br>bounded f32×f32 v181·f0·r0·req✓<br>pair f64×f64 v149·f0·r0·req✓<br>pair f32×f32 v181·f0·r0·req✓ |
| `distance.reassociated.dot` | `purrdf-core` | `reassociated::body::dot` (the `Reassociated` dot fold, compiled once per path and stored-width pair as `<path>::<query>_<row>::dot`) | `crates/rdf-core/src/distance/reassociated.rs:314` | `distance`, `knn_relation` | — | FR | — | fast-variant | regime 2 under its own contract (§3.2): inside each 64-element block the products are summed with `algebraic_mul` / `algebraic_add`, which licenses reassociation across vector accumulators and contraction to FMA; block sums are combined with plain `+` in ascending order. Reached by `Kernel::distance_reassociated` through a `Resolved<Reassociated>`, never by an exact entry point. Divergence, as its evidence text states: the last bits may differ from `Exact` and between dispatch paths or builds, the sign of a zero result is unspecified, and near-ties may order differently. Dispatch on x86_64 is AVX-512F, then AVX2+FMA, then SSE2, resolved once per scan; every target other than x86_64, aarch64 and wasm compiles the same `baseline` module as the `portable` path (image code 8), which no configuration here emits, so it has no count in this row and is executed instead by the `cross-arch` (i686) and `cross-arch-riscv64` CI jobs under the same tolerance tests; a hosted x86_64 runner guarantees only `sse2`, so the `avx2+fma` and `avx512f` paths are executed by the `avx512-sde` CI job, which runs the same tolerance tests, the exact-reference comparison and the kNN and HNSW suites under Intel SDE emulating a Sapphire Rapids processor and fails unless every x86_64 path is reported and executed; each path body is `#[inline(never)]` and non-generic, so the manifest requires exactly one copy of each measured symbol in every build graph. Measured for all four stored-width pairs on every compilation the configuration emits: `sse2` packs `addpd` / `mulpd` with no FMA; `avx2+fma` contracts into `ymm` `vfmadd231pd`; `avx512f` runs `zmm` FMA under generic tuning, but in an x86-64-v4 build that CPU model's tuning prefers 256-bit vectors, so there it is `ymm` FMA of the same shape as `avx2+fma` (measured, and required as `%ymm`); in x86-64-v3/v4 builds the `sse2` symbol is itself `ymm` FMA code; `neon` is `fmla v.2d`, and a neoverse-v1 build folds with SVE `fmla` and an `faddv` reduction | +simd128 (path `wasm-simd128`): `f64x2.mul` / `f64x2.add` lanes, no FMA (wasm has no fused op outside relaxed SIMD, which is never enabled) and no `relaxed_` op; f64×f64 unrolls the whole block into 33 `f64x2.mul`, while a pair with an `f32` operand keeps a rolled `f64x2.promote_low_f32x4` loop; baseline (path `wasm-scalar`): scalar `f64.mul` / `f64.add`, no vector work, still one out-of-line copy | sse2 f64×f64 v70·f0·r0·req✓<br>avx2+fma f64×f64 v13·f21·r0·req✓<br>avx512f f64×f64 v15·f11·r0·req✓<br>sse2 f64×f32 v104·f0·r0·req✓<br>avx2+fma f64×f32 v34·f21·r0·req✓<br>avx512f f64×f32 v26·f11·r0·req✓<br>sse2 f32×f64 v104·f0·r0·req✓<br>avx2+fma f32×f64 v34·f21·r0·req✓<br>avx512f f32×f64 v26·f11·r0·req✓<br>sse2 f32×f32 v138·f0·r0·req✓<br>avx2+fma f32×f32 v55·f21·r0·req✓<br>avx512f f32×f32 v37·f11·r0·req✓ | avx2+fma f64×f64 v13·f21·r0·req✓<br>avx512f f64×f64 v15·f11·r0·req✓<br>baseline f64×f64 v13·f21·r0·req✓<br>avx2+fma f64×f32 v34·f21·r0·req✓<br>avx512f f64×f32 v26·f11·r0·req✓<br>baseline f64×f32 v34·f21·r0·req✓<br>avx2+fma f32×f64 v34·f21·r0·req✓<br>avx512f f32×f64 v26·f11·r0·req✓<br>baseline f32×f64 v34·f21·r0·req✓<br>avx2+fma f32×f32 v55·f21·r0·req✓<br>avx512f f32×f32 v37·f11·r0·req✓<br>baseline f32×f32 v55·f21·r0·req✓ | avx2+fma f64×f64 v13·f21·r0·req✓<br>avx512f f64×f64 (v4 tuning) v13·f21·r0·req✓<br>baseline f64×f64 v13·f21·r0·req✓<br>avx2+fma f64×f32 v34·f21·r0·req✓<br>avx512f f64×f32 (v4 tuning) v34·f21·r0·req✓<br>baseline f64×f32 v34·f21·r0·req✓<br>avx2+fma f32×f64 v34·f21·r0·req✓<br>avx512f f32×f64 (v4 tuning) v34·f21·r0·req✓<br>baseline f32×f64 v34·f21·r0·req✓<br>avx2+fma f32×f32 v55·f21·r0·req✓<br>avx512f f32×f32 (v4 tuning) v55·f21·r0·req✓<br>baseline f32×f32 v55·f21·r0·req✓ | neon f64×f64 v12·f33·r0·req✓<br>neon f64×f32 v66·f34·r0·req✓<br>neon f32×f64 v66·f34·r0·req✓<br>neon f32×f32 v107·f34·r0·req✓ | neon f64×f64 (neoverse-v1) v9·f11·r0·req✓<br>neon f64×f32 (neoverse-v1) v10·f11·r0·req✓<br>neon f32×f64 (neoverse-v1) v10·f11·r0·req✓<br>neon f32×f32 (neoverse-v1) v11·f11·r0·req✓ | wasm-scalar f64×f64 v0·f0·r0<br>wasm-scalar f64×f32 v0·f0·r0<br>wasm-scalar f32×f64 v0·f0·r0<br>wasm-scalar f32×f32 v0·f0·r0 | wasm-simd128 f64×f64 v69·f0·r0·req✓<br>wasm-simd128 f64×f32 v13·f0·r0·req✓<br>wasm-simd128 f32×f64 v13·f0·r0·req✓<br>wasm-simd128 f32×f32 v16·f0·r0·req✓ |
| `distance.reassociated.sqeuclid` | `purrdf-core` | `reassociated::body::squared_euclidean_bounded` (compiled once per path and stored-width pair as `<path>::<query>_<row>::squared_euclidean_bounded`) | `crates/rdf-core/src/distance/reassociated.rs:338` | `distance`, `knn_relation`, `recall` | — | FR | — | fast-variant | the same contract: differences by `algebraic_sub`, squares summed by `algebraic_mul` / `algebraic_add` inside each 64-element block, block sums added in ascending order with a checkpoint after each block that tests non-finite before the bound. Block sums are non-negative, so every checkpoint is a true prefix of the full value and the bounded form agrees with the full one (`reassociated_bounded_agrees_with_full`). Reached by `Kernel::distance_reassociated` / `distance_bounded_reassociated`. The same divergence text and dispatch as `distance.reassociated.dot`, the `portable` path included; one copy per path | +simd128: `f64x2.sub` / `f64x2.mul` / `f64x2.add` in a rolled block loop, no FMA, no `relaxed_` op; baseline: scalar, no vector work, one out-of-line copy | sse2 f64×f64 v104·f0·r0·req✓<br>avx2+fma f64×f64 v34·f21·r0·req✓<br>avx512f f64×f64 v26·f11·r0·req✓<br>sse2 f64×f32 v138·f0·r0·req✓<br>avx2+fma f64×f32 v55·f21·r0·req✓<br>avx512f f64×f32 v37·f11·r0·req✓<br>sse2 f32×f64 v138·f0·r0·req✓<br>avx2+fma f32×f64 v55·f21·r0·req✓<br>avx512f f32×f64 v37·f11·r0·req✓<br>sse2 f32×f32 v172·f0·r0·req✓<br>avx2+fma f32×f32 v76·f21·r0·req✓<br>avx512f f32×f32 v48·f11·r0·req✓ | avx2+fma f64×f64 v34·f21·r0·req✓<br>avx512f f64×f64 v26·f11·r0·req✓<br>baseline f64×f64 v34·f21·r0·req✓<br>avx2+fma f64×f32 v55·f21·r0·req✓<br>avx512f f64×f32 v37·f11·r0·req✓<br>baseline f64×f32 v55·f21·r0·req✓<br>avx2+fma f32×f64 v55·f21·r0·req✓<br>avx512f f32×f64 v37·f11·r0·req✓<br>baseline f32×f64 v55·f21·r0·req✓<br>avx2+fma f32×f32 v76·f21·r0·req✓<br>avx512f f32×f32 v48·f11·r0·req✓<br>baseline f32×f32 v76·f21·r0·req✓ | avx2+fma f64×f64 v34·f21·r0·req✓<br>avx512f f64×f64 (v4 tuning) v34·f21·r0·req✓<br>baseline f64×f64 v34·f21·r0·req✓<br>avx2+fma f64×f32 v55·f21·r0·req✓<br>avx512f f64×f32 (v4 tuning) v55·f21·r0·req✓<br>baseline f64×f32 v55·f21·r0·req✓<br>avx2+fma f32×f64 v55·f21·r0·req✓<br>avx512f f32×f64 (v4 tuning) v55·f21·r0·req✓<br>baseline f32×f64 v55·f21·r0·req✓<br>avx2+fma f32×f32 v76·f21·r0·req✓<br>avx512f f32×f32 (v4 tuning) v76·f21·r0·req✓<br>baseline f32×f32 v76·f21·r0·req✓ | neon f64×f64 v48·f33·r0·req✓<br>neon f64×f32 v107·f34·r0·req✓<br>neon f32×f64 v107·f34·r0·req✓<br>neon f32×f32 v148·f34·r0·req✓ | neon f64×f64 (neoverse-v1) v18·f11·r0·req✓<br>neon f64×f32 (neoverse-v1) v19·f11·r0·req✓<br>neon f32×f64 (neoverse-v1) v19·f11·r0·req✓<br>neon f32×f32 (neoverse-v1) v20·f11·r0·req✓ | wasm-scalar f64×f64 v0·f0·r0<br>wasm-scalar f64×f32 v0·f0·r0<br>wasm-scalar f32×f64 v0·f0·r0<br>wasm-scalar f32×f32 v0·f0·r0 | wasm-simd128 f64×f64 v13·f0·r0·req✓<br>wasm-simd128 f64×f32 v16·f0·r0·req✓<br>wasm-simd128 f32×f64 v16·f0·r0·req✓<br>wasm-simd128 f32×f32 v19·f0·r0·req✓ |
| `knn.norm` | `purrdf-core` | `Resolved::<Exact>::norm` (re-exported through `knn`; the fold of `norm_fold`) | `crates/rdf-core/src/distance/mod.rs:765` | `build` | — | FI | the PURREMB normative fold | leave | PURREMB §13.2; computed once per row at build and once per query, O(d), with no hot site. It is now the normative `norm_fold` itself rather than a second transcription of it, so there is one copy of the order in the workspace. Measured in `hnsw::builder::compute_norms` | scalar on both builds | v0·f0·r0 | v0·f0·r0 | v0·f0·r0 | v0·f0·r0 | v0·f0·r0 | v0·f0·r0 | v0·f0·r0 |
| `hnsw.search-build` | `purrdf-hnsw` | `search::search_layer`, `builder::build_graph` (each instantiated in `purrdf_hnsw` for `Exact` and `Reassociated`) | `crates/hnsw/src/search.rs:425` | `build`, `recall` | — | FR | — | fast-variant | the exact index runs on `Exact` (regime 3): every build, selection, search, decode and rebuild distance goes through an arithmetic resolved for its thread, the beam scores each expanded node's neighbours with one `distances_indexed` batch, and the image header records the arithmetic (`IMAGE_VERSION` 2). The fast variant is `HnswIndex<Reassociated>`, a separate type built by `HnswIndex::build_reassociated` under its own contract: every call site computes each pair through the `distance.reassociated.*` kernels, one out-of-line copy per dispatch path, so build, selection and search agree bitwise on one path. Divergence: last bits may differ from the exact index and between dispatch paths or builds, so near-tied candidates may link or rank differently; recall is graded against the exact oracle and meets the exact index's pinned recall on the conformance family. Recording point: the image header's arithmetic field holds the code of the path resolved at build, followed by the build's `BuildShape`, the implementation is `hnsw-reassociated-v2` with an evidence revision naming that path, and decode, rebuild verification and search run the recorded path wherever this process can run it (an SSE2 or AVX2+FMA image on an AVX-512F processor included), and are refused with `ArithmeticPathUnavailable` only on a process that cannot, with `ArithmeticBuildMismatch` in a build of another shape, and a rebuild that diverges under both is `ArithmeticRebuildDiverged` (§3.5). Both walks are instantiated here through the constructors that fill the index's `Compiled` walks, not in a downstream crate; the counts are the walk's own integer lane work (`i64x2.mul` and `i64x2.shr_u` under +simd128, the splitmix64 level-hash shape), and the distance work, FMA included, is measured in the kernel rows | integer lane ops in `build_graph` under +simd128; the distances are the `distance.exact.*` and `distance.reassociated.*` kernels, `f64x2` under +simd128 | search v0·f0·r0<br>build v0·f0·r0<br>search reassociated v0·f0·r0<br>build reassociated v0·f0·r0 | search v0·f0·r0<br>build v132·f0·r0<br>search reassociated v0·f0·r0<br>build reassociated v132·f0·r0 | search v0·f0·r0<br>build v46·f0·r0<br>search reassociated v0·f0·r0<br>build reassociated v46·f0·r0 | search v0·f0·r0<br>build v1·f0·r0<br>search reassociated v0·f0·r0<br>build reassociated v1·f0·r0 | search v0·f0·r0<br>build v49·f0·r0<br>search reassociated v0·f0·r0<br>build reassociated v49·f0·r0 | search v0·f0·r0<br>build v0·f0·r0<br>search reassociated v0·f0·r0<br>build reassociated v0·f0·r0 | search v0·f0·r0<br>build v83·f0·r0<br>search reassociated v0·f0·r0<br>build reassociated v83·f0·r0 |
| `hnsw.distance-cache` | `purrdf-hnsw` | `search::DistanceCache` | `crates/hnsw/src/search.rs:124` | `build`, `recall` | — | FR | a per-round `BTreeMap<usize, f64>` memo | leave | ordered-map lookups; not vector-shaped. Measured in `search_layer`, which inlines it | scalar on both builds | v0·f0·r0 | v0·f0·r0 | v0·f0·r0 | v0·f0·r0 | v0·f0·r0 | v0·f0·r0 | v0·f0·r0 |
| `text.bm25f` | `purrdf-text` | `PreparedQuery::contribution` | `crates/text/src/ranking.rs:392` | `search` | text-search | I | BM25F on `i128` fixed point with checked division | leave | the crate denies float arithmetic; 128-bit checked multiply and divide per field have no vector form | scalar on both builds | v0·f0·r0 | v0·f0·r0 | v0·f0·r0 | v0·f0·r0 | v0·f0·r0 | v0·f0·r0 | v0·f0·r0 |
| `retrieval.rrf` | `purrdf-retrieval` | `reciprocal_rank::contribution` | `crates/retrieval/src/reciprocal_rank.rs:143` | `fusion_frontier_alloc` | retrieval-fusion | I | one fixed-point checked division per ranked item | leave | 128-bit checked division, one item at a time | scalar on both builds | v0·f0·r0 | v0·f0·r0 | v0·f0·r0 | v0·f0·r0 | v0·f0·r0 | v0·f0·r0 | v0·f0·r0 |
| `retrieval.fusion-frontier` | `purrdf-retrieval` | `FusionStream::next`'s frontier, `SelectionIndex::insert` / `SelectionIndex::take` | `crates/retrieval/src/fusion_stream.rs:1951` | `on_demand_read`, `fusion_frontier_alloc`, `multimodal_on_demand_read` | retrieval-fusion | I | the frontier is a `BTreeMap<Signature, BTreeSet<RankKey>>`, re-filed on every pull, exclusion answer and emission | leave | ordered-map inserts and removals keyed by a fixed-point score, a rank and a term; nothing contiguous to put in a lane. `FusionStream<S>` and the on-demand read under it (`CallRead<D>::pull`) are generic over the stream and the dataset, so they are compiled only in the crate that names those types, and no crate of the audited graph does; the entry measures the frontier's own out-of-line methods, which every instantiation calls | scalar on both builds | take v0·f0·r0<br>insert v0·f0·r0 | take v0·f0·r0<br>insert v0·f0·r0 | take v0·f0·r0<br>insert v0·f0·r0 | take v0·f0·r0<br>insert v0·f0·r0 | take v0·f0·r0<br>insert v0·f0·r0 | take v0·f0·r0<br>insert v0·f0·r0 | take v0·f0·r0<br>insert v0·f0·r0 |
| `columnar.plain-codec` | `purrdf-columnar` | `encode_int64_plain` (from `ColumnValues::encode_plain`), `decode_int64_plain` (from `decode_plain`) | `crates/columnar/src/parquet.rs:1542` | `codec` | columnar-codec | I | — | rewrite | PLAIN `INT64` only (BYTE_ARRAY values are length-prefixed and variable, and keep their loop). The nullable column is no longer an `Option<i64>` array of structs (a discriminant word beside each payload, which made the copy a masked load that SSE2, AVX2, NEON and `simd128` refused); it is a presence bitmap (one bit per row in `u64` words, bits past the last row clear) beside a dense `Vec<i64>` of the present values, so the PLAIN body is that vector in little-endian bytes. Encode collects each value's `to_le_bytes` into exactly sized storage; decode checks the body length against the counted present rows (a short body is `Truncated` at the end offset of the first word that does not fit, a long one the trailing-bytes `Malformed`, the answers the per-value loop gave), then collects each word's `from_le_bytes`. On a little-endian target both are a plain word copy that LLVM's loop-idiom pass lowers to one `memcpy` call (`memory.copy` on wasm32), so the measured functions hold no vector work and the manifest requires the call instead; a big-endian target swaps each word. The present-row count is a popcount over the bitmap in `decode_page`, which vectorizes there (x86_64 `psadbw` v27, x86_64-v3/v4 `vpshufb` + `vpsadbw` v51, aarch64 `cnt` v18, neoverse-v1 v16). Definition levels are encoded from the bitmap's maximal runs, found a word at a time by `trailing_zeros`. The one-value-at-a-time encode and decode and the one-row-at-a-time definition-level encode are kept under test as the oracles: every null pattern (all null, none, alternating in both phases, seeded runs, three densities, a lone null at every position) at every length 0 to 70 and at 256, 1,000, 4,099 and 65,541 rows, output bytes compared, and every body short by 1 to 25 bytes, empty, or long by 1 to 17 bytes refused with the same error, beside its exact-length neighbour, which decodes; `crates/columnar/tests/golden_bytes.rs` pins the five files of a seeded dataset, both compressions, to digests taken from the `Option<i64>` encoder | +simd128: the same `memory.copy` in both directions; baseline: the same `memory.copy` | encode v0·f0·r0·req✓<br>decode v0·f0·r0·req✓ | encode v0·f0·r0·req✓<br>decode v0·f0·r0·req✓ | encode v0·f0·r0·req✓<br>decode v0·f0·r0·req✓ | encode v0·f0·r0·req✓<br>decode v0·f0·r0·req✓ | encode v0·f0·r0·req✓<br>decode v0·f0·r0·req✓ | encode v0·f0·r0·req✓<br>decode v0·f0·r0·req✓ | encode v0·f0·r0·req✓<br>decode v0·f0·r0·req✓ |
| `columnar.rle` | `purrdf-columnar` | `decode_definition_levels` (RLE / bit-packed hybrid) | `crates/columnar/src/parquet.rs:1486` | `codec` | columnar-codec | I | run headers are varints; each run's length decides where the next begins | leave | loop-carried run detection | scalar on both builds | v0·f0·r0 | v0·f0·r0 | v0·f0·r0 | v0·f0·r0 | v0·f0·r0 | v0·f0·r0 | v0·f0·r0 |
| `geo.exact-limbs` | `purrdf-geo` | `exact::mag_add`, `Int::gcd` (`gcd_u128`) | `crates/geo/src/exact.rs:180` | `relate` | geo-relate | I | limb carry chains and Euclid's algorithm | leave | each limb waits on the previous carry; the crate denies float arithmetic | scalar on both builds | mag_add v0·f0·r0<br>gcd v0·f0·r0 | mag_add v0·f0·r0<br>gcd v0·f0·r0 | mag_add v0·f0·r0<br>gcd v0·f0·r0 | mag_add v0·f0·r0<br>gcd v0·f0·r0 | mag_add v0·f0·r0<br>gcd v0·f0·r0 | mag_add v0·f0·r0<br>gcd v0·f0·r0 | mag_add v0·f0·r0<br>gcd v0·f0·r0 |
| `geo.relate` | `purrdf-geo` | `topology::relate::relate`, `segment::intersect` | `crates/geo/src/topology/relate.rs:147` | `relate` | geo-relate | I | the pairwise segment `intersect` loop runs exact `Rat` arithmetic | leave | arbitrary-precision rationals, not machine lanes | scalar on both builds | relate v0·f0·r0<br>intersect v0·f0·r0 | relate v0·f0·r0<br>intersect v0·f0·r0 | relate v0·f0·r0<br>intersect v0·f0·r0 | relate v0·f0·r0<br>intersect v0·f0·r0 | relate v0·f0·r0<br>intersect v0·f0·r0 | relate v0·f0·r0<br>intersect v0·f0·r0 | relate v0·f0·r0<br>intersect v0·f0·r0 |
| `geo.json-write-string` | `purrdf-geo` | `json::write_string` | `crates/geo/src/json.rs:537` | — | — | I | — | covered | a one-call writer over the workspace's one JSON string-escape law, `purrdf_iri::json_escape`, in its `ShortForms` spelling; the clean runs end at the law's scanner for that spelling (`iri.scan-json-string`), one out-of-line copy in `purrdf_iri`, measured there and repeated in this entry. The law's own bench is `iri_json_escape_short_forms` in `crates/iri/benches/parse.rs` | the scan: +simd128 packed `i8x16` compares, baseline scalar lanes; the writer: scalar on both builds | scan sse2 v9·f0·r0·req✓<br>call site v0·f0·r0 | scan avx2 v21·f0·r0·req✓<br>call site v0·f0·r0 | scan avx512 masks v17·f0·r0·req✓<br>call site v0·f0·r0 | scan neon v6·f0·r0·req✓<br>call site v0·f0·r0 | scan neon v6·f0·r0·req✓<br>call site v0·f0·r0 | scan scalar v0·f0·r0<br>call site v0·f0·r0 | scan simd128 v7·f0·r0·req✓<br>call site v0·f0·r0 |
| `sha2.sha256-compress` | `purrdf-core` | `ContentDigest::of` → `sha2::Sha256` compression | `crates/rdf-core/src/content_store.rs:37` | `authoring` | — | I | — | covered | sha2 dispatches at run time to its SHA-NI kernel on x86_64 (`sha256rnds2`, `sha256msg1`, `sha256msg2`); aarch64 and wasm32 run its portable compression, because its ARMv8 backend needs a Cargo feature this workspace does not enable | portable, scalar on both builds | sha-ni v126·f0·r0·req✓ | sha-ni v126·f0·r0·req✓ | sha-ni v122·f0·r0·req✓ | portable v0·f0·r0 | portable v0·f0·r0 | portable v0·f0·r0 | portable v0·f0·r0 |
| `blake3.hash-many-dispatch` | `purrdf-gts` | `wire::blake3_256` → `blake3` chunk-parallel hashing | `crates/gts/src/wire.rs:143` | `authoring`, `gts_ingest`, `gts_selected_blobs` | — | I | — | covered | blake3's x86_64 kernels are hand-written assembly and its NEON kernel is C, all built by its build script; rustc's asm proves the dispatch reaches every width (`blake3_hash_many_sse2` … `_avx512`, `_neon`) | portable on both builds: blake3's simd128 backend needs a Cargo feature this workspace does not enable | asm kernels v0·f0·r0·req✓ | asm kernels v0·f0·r0·req✓ | asm kernels v0·f0·r0·req✓ | neon (C) v0·f0·r0·req✓ | neon (C) v0·f0·r0·req✓ | portable v0·f0·r0 | portable v0·f0·r0 |
| `gts.cbor-writer` | `purrdf-gts` | `wire::Canonical` serialization (canonical CBOR) | `crates/gts/src/wire.rs:78` | `authoring`, `gts_ingest`, `gts_selected_blobs` | — | I | a recursive serializer over `ciborium::Value` with length-prefixed headers | leave | control flow and small writes; the bytes are identity, so the order of every write is the contract | scalar on both builds | v0·f0·r0 | v0·f0·r0 | v0·f0·r0 | v0·f0·r0 | v0·f0·r0 | v0·f0·r0 | v0·f0·r0 |
| `gts.mmr-json-escape` | `purrdf-gts` | `mmr::json_escape` (inlined into `Proof::to_json`) | `crates/gts/src/mmr.rs:374` | — | — | I | — | covered | a one-call writer over the workspace's one JSON string-escape law, `purrdf_iri::json_escape`, in its `Controls` spelling; the clean runs end at the law's scanner for that spelling (`iri.json-escape-controls`), one out-of-line copy in `purrdf_iri`, measured there and repeated in this entry. The writer inlines into `Proof::to_json`, which is its measured call site. The per-`char` escaper it replaced is the oracle; the law's own bench is `iri_json_escape_controls` in `crates/iri/benches/parse.rs` | the scan: +simd128 packed `i8x16` compares, baseline scalar lanes; the writer: scalar on both builds | scan sse2 v12·f0·r0·req✓<br>call site v0·f0·r0 | scan avx2 v11·f0·r0·req✓<br>call site v0·f0·r0 | scan avx512 masks v5·f0·r0·req✓<br>call site v0·f0·r0 | scan neon v10·f0·r0·req✓<br>call site v0·f0·r0 | scan neon v10·f0·r0·req✓<br>call site v0·f0·r0 | scan scalar v0·f0·r0<br>call site v0·f0·r0 | scan simd128 v9·f0·r0·req✓<br>call site v0·f0·r0 |
| `gts.replication-json-string` | `purrdf-gts` | `replication::json_string` | `crates/gts/src/replication.rs:349` | — | — | I | — | covered | a one-call writer over the workspace's one JSON string-escape law, `purrdf_iri::json_escape`, in its `Controls` spelling; the clean runs end at the law's scanner for that spelling (`iri.json-escape-controls`), one out-of-line copy in `purrdf_iri`, measured there and repeated in this entry. The per-`char` escaper it replaced is the oracle; the law's own bench is `iri_json_escape_controls` in `crates/iri/benches/parse.rs` | the scan: +simd128 packed `i8x16` compares, baseline scalar lanes; the writer: scalar on both builds | scan sse2 v12·f0·r0·req✓<br>call site v0·f0·r0 | scan avx2 v11·f0·r0·req✓<br>call site v0·f0·r0 | scan avx512 masks v5·f0·r0·req✓<br>call site v0·f0·r0 | scan neon v10·f0·r0·req✓<br>call site v0·f0·r0 | scan neon v10·f0·r0·req✓<br>call site v0·f0·r0 | scan scalar v0·f0·r0<br>call site v0·f0·r0 | scan simd128 v9·f0·r0·req✓<br>call site v0·f0·r0 |
| `wasm.term-marshal` | `purrdf-wasm` | `convert::term_value_into_rdf_term`, `query::select_row` | `crates/rdf-wasm/src/convert.rs:78` | `query_engine_reuse` | — | I | one owned `String` move per term into the JS-facing term | leave | allocation and enum dispatch per term; the string bytes cross the boundary through wasm-bindgen's copy | scalar on both builds | v0·f0·r0 | v0·f0·r0 | v0·f0·r0 | v0·f0·r0 | v0·f0·r0 | v0·f0·r0 | v0·f0·r0 |
| `slice.ownership` | `purrdf-slice` | `OwnershipAnalyzer::analyze`, `collect_term_iri_refs` | `crates/slice/src/ownership.rs:325` | `ownership` | — | I | `BTreeSet` and `BTreeMap` inserts per referenced IRI | leave | ordered-set pointer chasing. `collect_term_iri_refs` is measured in `inspect_rdf_dataset`, which inlines it | scalar on both builds | analyze v47·f0·r0<br>iri refs v0·f0·r0 | analyze v32·f0·r0<br>iri refs v19·f0·r0 | analyze v24·f0·r0<br>iri refs v22·f0·r0 | analyze v26·f0·r0<br>iri refs v0·f0·r0 | analyze v27·f0·r0<br>iri refs v0·f0·r0 | analyze v0·f0·r0<br>iri refs v0·f0·r0 | analyze v0·f0·r0<br>iri refs v0·f0·r0 |
| `slice.prefix-json-string` | `purrdf-slice` | `prefix_emit::json_string` | `crates/slice/src/prefix_emit.rs:135` | — | — | I | — | covered | a one-call writer over the workspace's one JSON string-escape law, `purrdf_iri::json_escape`, in its `Minimal` spelling; the clean runs end at the law's scanner for that spelling (`iri.scan-json-string`), one out-of-line copy in `purrdf_iri`, measured there and repeated in this entry. The law's own bench is `iri_json_escape_minimal` in `crates/iri/benches/parse.rs` | the scan: +simd128 packed `i8x16` compares, baseline scalar lanes; the writer: scalar on both builds | scan sse2 v9·f0·r0·req✓<br>call site v0·f0·r0 | scan avx2 v21·f0·r0·req✓<br>call site v0·f0·r0 | scan avx512 masks v17·f0·r0·req✓<br>call site v0·f0·r0 | scan neon v6·f0·r0·req✓<br>call site v0·f0·r0 | scan neon v6·f0·r0·req✓<br>call site v0·f0·r0 | scan scalar v0·f0·r0<br>call site v0·f0·r0 | scan simd128 v7·f0·r0·req✓<br>call site v0·f0·r0 |
| `slice.dsl-stats-json-string` | `purrdf-slice` | `dsl_stats_emit::json_string` (inlined into `render_stats`) | `crates/slice/src/dsl_stats_emit.rs:138` | — | — | I | — | covered | a one-call writer over the workspace's one JSON string-escape law, `purrdf_iri::json_escape`, in its `Ascii` spelling; the clean runs end at the law's scanner for that spelling (`iri.json-escape-ascii`), one out-of-line copy in `purrdf_iri`, measured there and repeated in this entry. The writer inlines into `render_stats`, which is its measured call site, and escapes as Python's `json.dumps` does; the law's own bench is `iri_json_escape_ascii` in `crates/iri/benches/parse.rs` | the scan: +simd128 packed `i8x16` compares, baseline scalar lanes; the writer: scalar on both builds | scan sse2 v12·f0·r0·req✓<br>call site v0·f0·r0 | scan avx2 v24·f0·r0·req✓<br>call site v0·f0·r0 | scan avx512 masks v18·f0·r0·req✓<br>call site v0·f0·r0 | scan neon v8·f0·r0·req✓<br>call site v0·f0·r0 | scan neon v8·f0·r0·req✓<br>call site v0·f0·r0 | scan scalar v0·f0·r0<br>call site v0·f0·r0 | scan simd128 v9·f0·r0·req✓<br>call site v0·f0·r0 |
| `capi.crate` | `purrdf-capi` | — | — | — | — | — | — | leave | a C ABI wrapper over the facade with no hot path of its own; `crates/rdf-capi/benches/` does not exist | — | — | — | — | — | — | — | — |
| `cdt.crate` | `purrdf-cdt` | — | — | — | — | — | — | leave | composite-datatype value types with no measured hot path; `crates/cdt/benches/` does not exist | — | — | — | — | — | — | — | — |
| `jsonschema.crate` | `purrdf-jsonschema` | — | — | — | — | — | — | leave | a JSON Schema validator whose inner loops are `regex` matching and `serde_json` value traversal, both dependency code; `crates/jsonschema/benches/` does not exist | — | — | — | — | — | — | — | — |
| `events.crate` | `purrdf-events` | — | — | — | — | — | — | leave | a zero-dependency event vocabulary with no hot path; `crates/rdf-events/benches/` does not exist | — | — | — | — | — | — | — | — |
| `validate.crate` | `purrdf-validate` | — | — | — | — | — | — | leave | a dispatch layer over `purrdf-shapes` and `purrdf-shex`, whose rows are above; `crates/validate/benches/` does not exist | — | — | — | — | — | — | — | — |
| `facade.multimodal-read` | `purrdf` | `retrieval::search` over the real `text::TextSearchRelation` and `hnsw::relation::HnswRelation` producers; the per-candidate exclusion lookup's text side, `TextSearchRelation::holdings` | `crates/text/src/relation.rs:1063` | `multimodal_on_demand_read` | — | I | a membership point lookup: a binary search of the subject side index, then one of the term dictionary and the document's postings per `(document, term)` | leave | the umbrella compiles none of this path: the bench reaches every function through its `pub use` re-exports. The fused read is `purrdf-retrieval`'s (`retrieval.fusion-frontier`, `retrieval.rrf`) over the text producer's BM25F (`text.bm25f`) and the HNSW beam (`hnsw.search-build`, `hnsw.distance-cache`, `distance.exact.dot`, `distance.exact.sqeuclid`), each measured in its own row. What the bench adds is the governed exclusion lookup, timed alone: on the text side `holdings`, out of line in `purrdf_text` and measured there, whose binary searches have no lane to fill; on the HNSW side `HnswSpace::row_of`, a binary search over canonical term order, and `HnswIndex::row_distance` for the emitted distance, the resolved-handle pair kernel `distance.exact.sqeuclid` measures. `HnswRelation<A>` and `HnswSpace<A>` are generic over the arithmetic and only the caller that registers one names it, so `row_of` has no copy in the audited graph | scalar on both builds | text lookup v0·f0·r0 | text lookup v0·f0·r0 | text lookup v0·f0·r0 | text lookup v0·f0·r0 | text lookup v0·f0·r0 | text lookup v0·f0·r0 | text lookup v0·f0·r0 |
| `bench.crate` | `purrdf-bench` | — | — | — | — | — | — | leave | bench-lane infrastructure, not a runtime path; `crates/bench/benches/` does not exist | — | — | — | — | — | — | — | — |
| `alloc-probe.crate` | `purrdf-alloc-probe` | — | — | — | — | — | — | leave | a counting allocator for the allocation benches, not a runtime path; `crates/alloc-probe/benches/` does not exist | — | — | — | — | — | — | — | — |
| `testkit.crate` | `purrdf-testkit` | — | — | — | — | — | — | leave | shared test support (goldens, temporary paths, frozen vectors, the `harness = false` runner, the property harness), not a runtime path; `crates/testkit/benches/` does not exist | — | — | — | — | — | — | — | — |
| `envelope-probe.crate` | `purrdf-envelope-probe` | — | — | — | — | — | — | leave | a resource-envelope measurement tool, not a runtime path; `crates/envelope-probe/benches/` does not exist | — | — | — | — | — | — | — | — |
| `sparql-conformance.crate` | `purrdf-sparql-conformance` | — | — | — | — | — | — | leave | the W3C conformance harness; its hot paths are the evaluator's rows; `crates/sparql-conformance/benches/` does not exist | — | — | — | — | — | — | — | — |
| `python.crate` | `purrdf-python` | — | — | — | — | — | — | leave | the Python binding over the facade; `bindings/python/benches/` does not exist | — | — | — | — | — | — | — | — |
<!-- simd-asm:sites:end -->

### 4.2 Bench to site cross-reference

Every bench file under `crates/*/benches/` (support modules excluded) and the §4.1
rows whose code it exercises. `make simd-asm` fails when a bench file has no row, a
row names no site, or a site id is not a §4.1 row.

<!-- simd-asm:benches:begin -->
| bench | sites |
|---|---|
| `crates/cli/benches/pack_input.rs` | cli.pack-input |
| `crates/cli/benches/pack_input_alloc.rs` | cli.pack-input |
| `crates/columnar/benches/codec.rs` | columnar.plain-codec, columnar.rle |
| `crates/datalog/benches/seminaive.rs` | datalog.relation-contains, datalog.gallop-leapfrog |
| `crates/entail/benches/chase.rs` | datalog.chase, entail.escape |
| `crates/entail/benches/classify.rs` | entail.classify |
| `crates/entail/benches/consistency.rs` | entail.classify |
| `crates/entail/benches/schema_scan.rs` | entail.classify |
| `crates/geo/benches/relate.rs` | geo.relate, geo.exact-limbs |
| `crates/gts/benches/authoring.rs` | gts.cbor-writer, blake3.hash-many-dispatch, sha2.sha256-compress |
| `crates/hnsw/benches/build.rs` | hnsw.search-build, distance.exact.sqeuclid, knn.norm, hnsw.distance-cache |
| `crates/hnsw/benches/recall.rs` | hnsw.search-build, distance.exact.dot, distance.exact.sqeuclid, distance.reassociated.sqeuclid, hnsw.distance-cache |
| `crates/iri/benches/parse.rs` | iri.find-first-of, iri.validate-component, iri.terminals-in-ranges, iri.scan-trivia, iri.scan-iri-body, iri.scan-json-string, iri.scan-xml, iri.json-escape-controls, iri.json-escape-ascii |
| `crates/json/benches/ordered_json.rs` | json.parser-string, json.parser-whitespace |
| `crates/json/benches/ordered_json_alloc.rs` | json.parser-string |
| `crates/json/benches/ordered_json_corpus.rs` | json.parser-string, json.parser-whitespace |
| `crates/markdown/benches/slicer.rs` | markdown.lines |
| `crates/purrdf/benches/multimodal_on_demand_read.rs` | facade.multimodal-read, retrieval.fusion-frontier, retrieval.rrf, text.bm25f, hnsw.search-build, hnsw.distance-cache, distance.exact.dot, distance.exact.sqeuclid |
| `crates/rdf/benches/flat_view_canon.rs` | core.canon-escape |
| `crates/rdf/benches/gts_ingest.rs` | gts.cbor-writer, blake3.hash-many-dispatch, memchr.rdf-line-split |
| `crates/rdf/benches/gts_selected_blobs.rs` | gts.cbor-writer, blake3.hash-many-dispatch |
| `crates/rdf/benches/jsonld_alloc.rs` | json.parser-string |
| `crates/rdf/benches/native_codecs.rs` | memchr.rdf-line-split, rdf.escape-scan, rdf.sort-canonical, sparql.lexer-trivia |
| `crates/rdf/benches/projections.rs` | rdf.projections, rdf.csvw-percent-encode |
| `crates/rdf/benches/stream_parse_alloc.rs` | memchr.rdf-line-split, sparql.lexer-trivia |
| `crates/rdf/benches/viz_layout.rs` | rdf.viz-layout |
| `crates/rdf-core/benches/distance.rs` | distance.exact.dot, distance.exact.sqeuclid, distance.reassociated.dot, distance.reassociated.sqeuclid |
| `crates/rdf-core/benches/intern_absoluteness.rs` | core.intern, iri.find-first-of |
| `crates/rdf-core/benches/intern_content_id.rs` | core.intern, core.content-id-hex, core.hex-lower |
| `crates/rdf-core/benches/ir_layout.rs` | core.dataset-scan-filter, core.intern |
| `crates/rdf-core/benches/mutable.rs` | core.dataset-scan-filter |
| `crates/rdf-core/benches/pack_bits.rs` | core.pack-select-in-word, core.pack-rank1, core.pack-intvector-get |
| `crates/rdf-core/benches/pack_index_compare.rs` | core.pack-triples-intersect |
| `crates/rdf-core/benches/pack_query.rs` | core.pack-dict-prefix, core.pack-triples-intersect |
| `crates/rdf-core/benches/pack_restore_alloc.rs` | cli.pack-input, core.pack-triples-intersect, core.pack-intvector-get |
| `crates/rdf-core/benches/purremb.rs` | core.purremb-f32-decode, core.purremb-norm |
| `crates/rdf-core/benches/purremb_alloc.rs` | core.purremb-f32-decode, core.purremb-norm |
| `crates/rdf-core/benches/shared_views.rs` | core.dataset-scan-filter |
| `crates/rdf-core/benches/sssom.rs` | core.sssom-tsv |
| `crates/rdf-core/benches/xsd_regex.rs` | core.xsd-regex-scan |
| `crates/rdf-wasm/benches/query_engine_reuse.rs` | wasm.term-marshal, eval.bgp-join-probe |
| `crates/retrieval/benches/fusion_frontier_alloc.rs` | retrieval.fusion-frontier, retrieval.rrf |
| `crates/retrieval/benches/on_demand_read.rs` | retrieval.fusion-frontier, retrieval.rrf, distance.exact.dot, distance.exact.sqeuclid |
| `crates/shapes/benches/canonical_sort.rs` | shapes.finish-report |
| `crates/shapes/benches/pattern_lookup.rs` | shapes.path-eval, shapes.frontier-dedup |
| `crates/shapes/benches/pattern_validate.rs` | core.xsd-regex-scan |
| `crates/shapes/benches/pydantic_package.rs` | shapes.pydantic-emit |
| `crates/shapes/benches/schema_surface.rs` | shapes.schema-compile |
| `crates/shapes/benches/shacl_product_alloc.rs` | shapes.path-eval, shapes.finish-report |
| `crates/shapes/benches/shacl_product_reuse.rs` | shapes.path-eval, shapes.finish-report |
| `crates/shapes/benches/shared_views.rs` | shapes.path-eval |
| `crates/shapes/benches/validate.rs` | shapes.path-eval, shapes.finish-report |
| `crates/shex/benches/pattern_validate.rs` | core.xsd-regex-scan, shex.char-count |
| `crates/shex/benches/prepared_validate.rs` | shex.matcher |
| `crates/slice/benches/ownership.rs` | slice.ownership |
| `crates/sparql-algebra/benches/tokenize.rs` | memchr.sparql-lex-string, sparql.lexer-trivia, sparql.lexer-iri, sparql.lexer-cursor, iri.scan-trivia, iri.scan-iri-body |
| `crates/sparql-algebra/benches/tokenize_values.rs` | memchr.sparql-lex-string, sparql.lexer-trivia, sparql.lexer-iri, sparql.lexer-cursor, iri.scan-trivia, iri.scan-iri-body |
| `crates/sparql-algebra/benches/update_reparse.rs` | memchr.sparql-lex-string, sparql.lexer-trivia, sparql.lexer-iri, sparql.lexer-cursor, iri.scan-trivia, iri.scan-iri-body |
| `crates/sparql-eval/benches/construct_builder.rs` | eval.bgp-join-probe |
| `crates/sparql-eval/benches/cost_based_bgp_planner.rs` | eval.bgp-cost-planner, eval.bgp-join-probe |
| `crates/sparql-eval/benches/knn_relation.rs` | distance.exact.dot, distance.exact.sqeuclid, distance.reassociated.dot, distance.reassociated.sqeuclid |
| `crates/sparql-eval/benches/exists_decorrelation.rs` | eval.solution-compatible, eval.bgp-join-probe |
| `crates/sparql-eval/benches/governed_eval.rs` | eval.bgp-join-probe |
| `crates/sparql-eval/benches/lateral_service.rs` | eval.solution-compatible |
| `crates/sparql-eval/benches/lateral_substitution.rs` | eval.solution-compatible |
| `crates/sparql-eval/benches/paged_cross_page_bgp.rs` | core.paged-map-quad, eval.bgp-join-probe |
| `crates/sparql-eval/benches/path_range.rs` | eval.bgp-join-probe, core.dataset-scan-filter |
| `crates/sparql-eval/benches/prepared_admission.rs` | eval.bgp-join-probe |
| `crates/sparql-eval/benches/prepared_reuse.rs` | eval.bgp-join-probe |
| `crates/sparql-eval/benches/query_eval.rs` | eval.bgp-join-probe, eval.bgp-cost-planner, eval.solution-compatible, eval.aggregate-fold |
| `crates/sparql-eval/benches/regex_eval.rs` | core.xsd-regex-scan, eval.regex-row-alloc |
| `crates/sparql-eval/benches/relation_attestation_alloc.rs` | eval.solution-compatible, eval.bgp-join-probe |
| `crates/sparql-eval/benches/solution_row.rs` | eval.solution-compatible |
| `crates/sparql-results/benches/graph_serialize.rs` | results.csv-field, results.json-escape, results.xml-escape |
| `crates/text/benches/search.rs` | text.bm25f |
| `crates/xsd/benches/lexical.rs` | xsd.whitespace, xsd.canonical-double |
<!-- simd-asm:benches:end -->

### 4.3 Crate coverage

Every workspace member of the root `Cargo.toml` and the §4.1 rows that cover it. A
member with a `benches/` directory has function rows with manifest entries; a
member without one has a crate-level row citing that absence. `make simd-asm` fails
when a member has no row.

| Member | Directory | `benches/` | §4.1 rows |
|---|---|---|---|
| `purrdf-python` | `bindings/python` | absent | `python.crate` |
| `purrdf-alloc-probe` | `crates/alloc-probe` | absent | `alloc-probe.crate` |
| `purrdf-bench` | `crates/bench` | absent | `bench.crate` |
| `purrdf-cdt` | `crates/cdt` | absent | `cdt.crate` |
| `purrdf-cli` | `crates/cli` | present | `cli.pack-input` |
| `purrdf-columnar` | `crates/columnar` | present | `columnar.plain-codec`, `columnar.rle` |
| `purrdf-datalog` | `crates/datalog` | present | `datalog.relation-contains`, `datalog.gallop-leapfrog`, `datalog.chase` |
| `purrdf-entail` | `crates/entail` | present | `entail.escape`, `entail.classify` |
| `purrdf-envelope-probe` | `crates/envelope-probe` | absent | `envelope-probe.crate` |
| `purrdf-geo` | `crates/geo` | present | `geo.exact-limbs`, `geo.relate`, `geo.json-write-string` |
| `purrdf-gts` | `crates/gts` | present | `blake3.hash-many-dispatch`, `gts.cbor-writer`, `gts.mmr-json-escape`, `gts.replication-json-string` |
| `purrdf-hnsw` | `crates/hnsw` | present | `hnsw.search-build`, `hnsw.distance-cache` |
| `purrdf-iri` | `crates/iri` | present | `iri.find-first-of`, `iri.scan-trivia`, `iri.scan-iri-body`, `iri.scan-json-string`, `iri.json-escape-controls`, `iri.json-escape-ascii`, `iri.scan-xml`, `iri.validate-component`, `iri.terminals-in-ranges` |
| `purrdf-json` | `crates/json` | present | `json.parser-string`, `json.parser-whitespace` |
| `purrdf-jsonschema` | `crates/jsonschema` | absent | `jsonschema.crate` |
| `purrdf-markdown` | `crates/markdown` | present | `markdown.lines` |
| `purrdf` | `crates/purrdf` | present | `facade.multimodal-read` |
| `purrdf-rdf` | `crates/rdf` | present | `memchr.rdf-line-split`, `rdf.escape-scan`, `rdf.sort-canonical`, `rdf.viz-layout`, `rdf.projections`, `rdf.csvw-percent-encode` |
| `purrdf-capi` | `crates/rdf-capi` | absent | `capi.crate` |
| `purrdf-core` | `crates/rdf-core` | present | the sixteen `core.*` rows, `distance.exact.dot`, `distance.exact.sqeuclid`, `distance.reassociated.dot`, `distance.reassociated.sqeuclid`, `knn.norm` and `sha2.sha256-compress` |
| `purrdf-events` | `crates/rdf-events` | absent | `events.crate` |
| `purrdf-wasm` | `crates/rdf-wasm` | present | `wasm.term-marshal` |
| `purrdf-retrieval` | `crates/retrieval` | present | `retrieval.rrf`, `retrieval.fusion-frontier` |
| `purrdf-shapes` | `crates/shapes` | present | `shapes.frontier-dedup`, `shapes.path-eval`, `shapes.finish-report`, `shapes.pydantic-emit`, `shapes.schema-compile` |
| `purrdf-shex` | `crates/shex` | present | `shex.char-count`, `shex.matcher` |
| `purrdf-slice` | `crates/slice` | present | `slice.ownership`, `slice.prefix-json-string`, `slice.dsl-stats-json-string` |
| `purrdf-sparql-algebra` | `crates/sparql-algebra` | present | `memchr.sparql-lex-string`, `sparql.lexer-trivia`, `sparql.lexer-iri`, `sparql.lexer-cursor` |
| `purrdf-sparql-conformance` | `crates/sparql-conformance` | absent | `sparql-conformance.crate` |
| `purrdf-sparql-eval` | `crates/sparql-eval` | present | `eval.regex-row-alloc`, `eval.solution-compatible`, `eval.bgp-cost-planner`, `eval.bgp-join-probe`, `eval.aggregate-fold` |
| `purrdf-sparql-results` | `crates/sparql-results` | present | `results.xml-escape`, `results.csv-field`, `results.json-escape` |
| `purrdf-testkit` | `crates/testkit` | absent | `testkit.crate` |
| `purrdf-text` | `crates/text` | present | `text.bm25f` |
| `purrdf-validate` | `crates/validate` | absent | `validate.crate` |
| `purrdf-xsd` | `crates/xsd` | present | `xsd.whitespace`, `xsd.canonical-double` |

---

## 5. MSRV decision

**The rule.** The MSRV moves from 1.96 to 1.98 if and only if two things hold: the
reassociated arithmetic (regime 2) takes a real win the exact law cannot, and §3 can
express that arithmetic as a separate contract without weakening the exact one.

**Both hold.** Regime 3 already vectorizes the exact fold, but it cannot reassociate
across its fixed tree and it can never fuse a multiply into an add: FMA and free
reassociation are exactly what the exact contract forbids (§2.4). The only stable way
to grant them for one operation is the `algebraic_*` family, stable from Rust 1.98.
That win is taken by the reassociated distance kernels, and nowhere else: the
reassociated `dot` and `squared_euclidean` behind `Kernel::distance_reassociated`,
the reassociated kNN relation, and the reassociated HNSW build and search. §3 expresses
them as the `Reassociated` implementation of `Arithmetic`, with their own names,
their own evidence and their own recorded identifiers, and the static scan keeps
them off every identity path. The MSRV therefore moves to 1.98.

**What moved with it.** `rust-version` in the workspace manifest, the CI `msrv` job
(its toolchain, both `RUSTUP_TOOLCHAIN` values and the assertion that the resolved
compiler really is 1.98), `clippy.toml`, the README badge and MSRV policy, the
contributor and agent guides, and every crate README that states the floor. The
floor is proven in both directions: a 1.98 compiler checks the workspace, and a 1.97
compiler, with cargo's `rust-version` guard set aside, rejects the crate on
`float_algebraic` itself (E0658), so the floor comes from the code, not the manifest.

**The consequence, accepted deliberately.** 1.98 was the current stable release when
the floor moved, so a consumer has no headroom: the oldest compiler PurRDF supports is
the newest one available. Every other capability this document relies on (the x86,
aarch64 and wasm intrinsics, safe `#[target_feature]` functions, `as_chunks`) was
already inside the previous floor; `algebraic_*` alone required the move.
